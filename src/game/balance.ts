/**
 * 밸런스 튜너블을 한곳에 모은다.
 *
 * 값이 코드 여기저기 흩어져 있으면 스윕으로 최적점을 찾을 수 없다.
 * scripts/balance-sim.mjs가 이 객체를 덮어쓰고 도달 웨이브 분포를 측정한다.
 */
export const BALANCE = {
  /** 시작 생선 */
  startGold: 8,
  /** 웨이브 클리어 보상 = goldBase + wave * goldPerWave */
  goldBase: 5,
  goldPerWave: 1.5,

  /**
   * 상점 칸이 주는 생선.
   *
   * 전투 보상(goldBase + wave*goldPerWave)보다 조금 적어야 한다. 더 주면
   * 싸우지 않는 쪽이 언제나 이득이라 지도가 "상점만 밟는 길 찾기"가 된다.
   */
  shopNodeGold: 5,
  /**
   * 정찰이 주는 다음 보스전 여분 회피.
   *
   * 기본 회피가 6이므로 +2는 3분의 1이다. 보스전이 예고 열 번쯤 도는 것을
   * 감안하면 두 번을 더 읽을 수 있다는 뜻이다.
   */
  scoutDodgeBonus: 2,
  /** 정예를 넘었을 때의 생선 배수. 저격대는 통과율이 낮으므로 값을 해야 한다. */
  eliteGoldMul: 1.6,

  /**
   * 적 스탯 배수 = enemyScale^(wave-1).
   * 스윕으로 잡았다. 같은 중앙값(13)을 내는 조합이 여럿이었는데, 분포 하한이
   * 높은 쪽을 골랐다 — 하한이 낮으면 짧은 런이 잦아 좌절이 늘어난다.
   */
  // 1.24 → 1.28. 회피가 실제로 통하게 되면서 판 전체가 쉬워졌다(중앙값 12 → 25).
  // 차지를 줄이고 예고 피해를 올려 맞출 수도 있었지만 그러면 판이 보스전 한
  // 순간으로 수렴해 유물 축이 죽는다. 전 웨이브에 걸리는 이 손잡이로 되돌리면
  // 중앙값도 12로 돌아오고 유물 격차도 3.3웨이브로 산다.
  enemyScale: 1.28,
  /**
   * 적 수 = ceil(wave / enemyCountDivisor), 보유 한도가 상한.
   * 1.4에서 올렸다 — 초반 사망이 19.5%로 너무 높았다.
   */
  enemyCountDivisor: 1.45,

  /** 개별 고양이 레벨당 배수 */
  levelScale: 1.4,
  /**
   * 강화 비용 = upgradeCostBase * upgradeCostGrowth^(level-1)
   *
   * 비용이 선형이면 효과(지수)를 절대 따라잡지 못해 후반에 무한히 강해진다.
   * 실제로 선형 비용 버전은 시뮬 12%가 60웨이브 상한에 걸려 런이 끝나지 않았다.
   */
  /**
   * 강화 비용의 증가율.
   *
   * 1.45에서 1.32로 내렸다. 이유는 값이 아니라 **부등호**다 — 비용 지수가 성장
   * 지수(levelScale 1.4)보다 크면 생선을 아무리 벌어도 전력이 G^0.9로만 큰다.
   * 뒤집으면 같은 생선으로 더 멀리 간다.
   *
   * 효과는 정직하게 말해 작다. W20 전력비 0.56 → 0.63. 적이 1.24^w로 크는 한
   * 나머지는 전부 반올림 오차다 — 판 길이를 정말 옮기려면 enemyScale을 만져야
   * 한다는 것이 이번 측정의 결론이다.
   */
  upgradeCostBase: 4,
  upgradeCostGrowth: 1.32,

  /**
   * 웨이브를 넘길 때마다 살아남은 전원이 얻는 영구 배수.
   * 이게 없으면 적은 전체가 복리로 크는데 플레이어는 웨이브당 한 마리만
   * 강화하게 되어(9마리 중 1마리 ×1.4 ≈ 팀 전력 +4%) 구조적으로 따라잡힌다.
   */
  veterancy: 1.14,

  /** 시작 고양이 수 */
  starterCount: 3,

  /**
   * 보유 한도 = min(unitCapMax, unitCapBase + floor(wave / unitCapEvery)).
   *
   * 보드가 5x5(25칸)가 되면서 칸 수로는 아무것도 제한되지 않는다. TFT가 레벨로
   * 배치 수를 묶는 것과 같은 이유로 한도를 따로 둔다. 5x5는 칸이 늘어난 게
   * 아니라 **배치 자유도**다.
   */
  /**
   * 영입 비용이 오르기 시작하는 팀 크기, 그리고 그 뒤의 증가율.
   *
   * **강화는 지수인데 영입은 고정이었다.** 강화 비용은 예전에 선형이라 후반에
   * 무한히 강해지는 결함을 냈고 그때 지수로 고쳤는데, 영입은 그대로 남았다 —
   * 8번째 고양이가 2번째와 같은 값이다. 깊게 가는 것은 비싸고 넓게 가는 것은
   * 싸니, "무엇을 사는가"보다 "몇 마리를 사는가"가 이기게 된다. 실제로 구매
   * 깊이가 1.4웨이브(기준 1.5)로 미달이었고, 무작위 구매가 몸으로 때워서
   * 최선에 붙었다.
   *
   * 문턱 5는 **판의 수용력**에서 왔다. 전투 중 이웃 간격을 재 보니 5마리에서
   * 1.38칸, 9마리에서 1.11칸인데 겹침 최소 간격(`SEPARATION`)이 1.0이다 —
   * 6마리를 넘으면 밀어내기가 포화해서 더 세울 자리가 없다. 그때부터 값이
   * 오른다. 한도(10)는 그대로 둔다. **못 가게 막는 것이 아니라 값을 받는 것**이
   * 결정이다 — 넓게 갈 수 있고, 대신 그만큼 강화를 포기한다.
   */
  recruitFreeUpTo: 5,
  recruitGrowth: 1.35,

  unitCapBase: 4,
  unitCapEvery: 2,
  unitCapMax: 10,

  /**
   * 보스.
   *
   * 예전에는 statBoost 1.35가 run.ts에 하드코딩돼 있어 스윕 대상이 아니었다.
   * 보스 통과율이 개입 강도의 판정 기준이므로, 그 통과율을 조절할 손잡이가
   * 여기 있어야 balance-sweep이 2축으로 훑을 수 있다.
   */
  /**
   * 보스 체력 배수. 첫 보스와 후반 보스에 다른 값을 쓴다.
   *
   * 같은 배수라도 5웨이브에서는 팀이 다섯 마리뿐이라 훨씬 두껍게 느껴진다.
   * 32로 고정했더니 p25가 5로 무너졌다 — 정확히 첫 보스에서 4분의 1이 죽었고,
   * 그건 "보스는 벽이 아니라 사건"이라는 이 저장소의 오래된 결정에 어긋난다.
   */
  bossHpMulFirst: 62,
  bossHpMul: 142,
  /** 배수가 First에서 Mul까지 올라가는 데 걸리는 웨이브 수 */
  /** 몇 번째 보스에서 최대 강도에 닿는가 */
  bossRampCount: 8,
  /**
   * 보스 평타는 **약하다**. 위험은 예고 광역에서 온다.
   *
   * 체력으로 길이를, 예고로 위험을 만들어야 둘을 따로 조절할 수 있다.
   * 평타를 세게 두면 길이와 난이도가 한 손잡이에 묶여, 전투를 늘리면 반드시
   * 어려워지고 쉽게 만들면 반드시 짧아진다.
   *
   * 보스전을 12초에서 25초로 늘리면서 0.8에서 0.28로 더 낮췄다. 전투가
   * 길어지면 평타에 노출되는 시간도 그만큼 길어지기 때문이다 — 체력만 올렸을
   * 때 통과율이 84%에서 66%로 떨어진 것이 그 탓이었고, 원인은 예고가 아니라
   * 오래 맞는 것이었다.
   */
  /**
   * 보스 평타 배수. **첫 보스와 후반 보스에 다른 값을 쓴다** — 체력·예고 피해와
   * 같은 방식이다.
   *
   * 0.28 고정이었을 때 W6 보스 공격력이 10이고 우리 고양이 체력이 400~700이라
   * 한 마리 죽이는 데 40번을 때려야 했다. 즉 **평타가 사실상 없었고, 그래서
   * 탱킹도 없었다.** `pickTarget`이 가장 가까운 적을 고르므로 "앞에 누구를
   * 두느냐"가 작동할 구조는 이미 있는데, 맞아도 안 아프니 아무도 신경 쓰지
   * 않았다. 오토배틀러에서 배치의 첫째 이유가 비어 있던 셈이다.
   *
   * 그렇다고 고정값을 올리면 첫 보스가 벽이 된다 — 2.0으로 올렸더니 3웨이브에
   * 팀이 4~5마리뿐이라 중앙값이 12에서 3으로 주저앉았다. 체력이 62→142로
   * 올라가는 것과 같은 이유로 여기도 램프가 필요하다.
   */
  bossAtkMulFirst: 0.28,
  bossAtkMul: 0.28,
  bossEscortCount: 2,
  /** 저격수 체력 배수. 보스보다 훨씬 얇다 — 벽이 아니라 연습이다. */
  sniperHpMul: 5,
  /** 저격 웨이브에 주는 회피 횟수. 보스(6)보다 적다 */
  sniperDodgeCharges: 2,
  /**
   * 예고 광역이 적중했을 때 **최대 체력 대비** 피해 비율.
   *
   * 절대값이 아니라 비율인 이유: 팀 구성과 웨이브에 상관없이 "뭉쳐 있으면
   * 아프다"가 일정하게 성립해야 배치가 결정이 된다.
   *
   * 이 값이 보스 통과율의 주 손잡이다.
   *
   * 체력 배수와 같은 램프를 건다. 첫 보스에서 팀은 다섯 마리뿐이라 흩어질
   * 자리조차 없는데, 후반과 같은 비율을 맞으면 p25가 5로 무너진다.
   * 첫 보스는 기믹을 **가르치는** 자리이지 죽이는 자리가 아니다.
   */
  /**
   * 보스전 한 판에 쓸 수 있는 회피 횟수.
   *
   * 체력 문턱 수(6)와 같은 6이었다. 그때는 회피가 실제로는 39%만 통했으므로
   * "전부 막을 수 있다"가 이름뿐이었는데, 개입이 100% 통하게 고치자 예고가
   * 통째로 무력화돼 중앙값이 12에서 25로 튀었다.
   *
   * 그래서 문턱보다 **적게** 준다. 여섯 번 중 둘만 막을 수 있으므로 "어느
   * 것을 막을까"가 매 보스전의 결정이 된다 — 버튼을 하나로 합치면서 사라진
   * 자리(무엇을 누를까)를 여기가 대신 받는다.
   */
  dodgeCharges: 2,
  /**
   * 약점 공격 한 번이 주는 피해(보스 최대 체력 대비).
   *
   * 연타할수록 콤보가 쌓여 배수가 붙는다. 창이 3초이고 스텝당 하나만 소비하니
   * 최대 30번, 실제로는 사람 손 기준 20번쯤이다. 창 하나를 완벽히 살리면
   * 보스 체력의 15~20%를 깎는 정도로 잡는다 — 판을 뒤집지는 않되
   * "잘 눌렀다"가 눈에 보이는 크기다.
   */
  strikeFrac: 0.0024,
  /** 콤보 한 단계당 배수 증가분과 상한 */
  strikeComboStep: 0.12,
  strikeComboMax: 12,
  /*
   * `gatherMissMul`은 지웠다. 뭉침이 soak(들어온 것끼리 나눠 맞기)에서
   * **대피소**(안은 안 맞고 밖만 맞는다)로 바뀌면서 "놓쳤을 때"라는 별도
   * 국면이 사라졌다 — 이제 밖에 있는 것은 붉은 장판 안에 있는 것과 똑같이
   * `telegraphDmg`를 맞는다. 규칙이 하나면 손잡이도 하나다.
   */
  /**
   * 예고 피해의 첫 보스 값과 최대값.
   *
   * 0.135 / 1.15에서 옮겼다. **수치가 아니라 램프 기준을 고친 결과다.**
   * 예고는 `bossesSeen`(지금 서 있는 보스도 세므로 첫 보스가 1)을, 체력은
   * 보스 순번(첫 보스가 0)을 보고 있었다. 즉 예고만 램프에서 한 칸 앞서 있었고,
   * 첫 보스가 실제로 맞던 값은 0.135가 아니라 `0.135 + 1.015×(1/8) = 0.262`였다.
   *
   * 램프를 순번으로 통일하면서 상수가 실제 값을 말하게 옮긴다 — 두 값은 옛 곡선을
   * 그대로 재현한다(First' = 옛 첫 보스 값, Dmg' = First' + 옛 기울기 × 8).
   * 통일만 하고 상수를 그대로 뒀더니 중앙값이 12에서 17로 뛰어 목표대(10~15)를
   * 벗어났다. 버그 수정이 밸런스를 조용히 옮기면 안 된다.
   *
   * 회피를 100% 통하게 고친 뒤에도 이 값은 안 건드렸다. 한 번 올려 봤는데
   * (0.29/1.4) 중앙값은 맞았지만 **유물 축이 무너졌다** — 예고 한 대가
   * 무거워질수록 판이 보스전 한 순간으로 수렴하고, 일반 전투력을 올리는
   * 유물은 잴 자리를 잃는다. 난이도는 `enemyScale`로 되찾았다.
   */
  telegraphDmgFirst: 0.262,
  telegraphDmg: 1.277,
  /**
   * 예고 피해 중 **고정값이 차지하는 몫** (0이면 전부 비율, 1이면 전부 고정).
   *
   * 비율 피해(`maxHp × frac`)는 웨이브·팀 구성과 무관하게 "뭉치면 아프다"가
   * 성립한다는 장점이 있었다. 그런데 그 편의의 대가가 **직업 구분 무효화**였다 —
   * 전사(체력 190)와 마법사(94)가 예고 한 번에 같은 비율을 잃으니, 죽기까지
   * 걸리는 예고 횟수가 완전히 같다. 그래서 보스전에서 전사를 앞에 세울 이유가
   * 없고, 그게 배치 깊이가 0.2인 뿌리다.
   *
   * 고정 몫을 섞으면 체력이 처음으로 값을 한다. 기준은 보스 공격력이라
   * 웨이브에 따라 같이 커진다.
   */
  telegraphFlatShare: 0,
  /** 고정 몫의 크기 = 보스 공격력 × 이 값 */
  telegraphFlatMul: 36,

  /**
   * 자기 줄로 돌아가려는 힘 (초당 비율).
   *
   * **배치가 왜 무의미했는지의 뿌리를 여기서 잡는다.** 이동 규칙이 "목표를
   * 향해 직선으로 걷고 사거리에서 멈춘다"뿐이라, 최종 위치가 (목표 위치,
   * 자기 사거리)만으로 정해졌다 — 시작 자리의 기억이 남지 않는다. 실측으로
   * 대형별 퍼짐이 0.91·2.16·2.55로 시작해 예고 시점엔 전부 1.67~1.70으로
   * 수렴했다. 플레이어가 정한 대형이 몇 초 만에 지워진 것이다.
   *
   * 전진(가로)은 건드리지 않는다. **옆으로 흐르는 것만** 붙잡는다 — 자기 행에서
   * 벗어난 만큼 초당 이 비율로 되돌린다. 목표를 향한 세로 이동과 힘겨루기를
   * 하므로 완전히 고정되지는 않고, 교전 거리에서는 목표 쪽이 이긴다.
   *
   * 0이면 예전 동작 그대로다.
   */
  lanePull: 1.6,
};

export type Balance = typeof BALANCE;
