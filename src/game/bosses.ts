import type { Breed, TelegraphShape } from "./types.ts";

/**
 * 보스.
 *
 * 예전 보스 웨이브는 "적 수를 반으로 줄이고 스탯 1.35배"였다. 그래서 통과율이
 * 96%로 이 게임에서 가장 안전한 웨이브 중 하나였고, 벽이 아니라 사건이라는
 * 원래 의도에는 맞았지만 **잴 것이 없었다** — 개입을 만들어도 통과율을 15%p
 * 올릴 여지가 3%p밖에 없었다.
 *
 * 그래서 보스를 **한 마리의 큰 것**으로 다시 만든다. 3x3 칸을 차지하고 체력이
 * 두껍고 오래 싸운다. 통과율을 조절할 손잡이(`balance.ts`의 보스 항목)를 두어
 * 개입의 값을 측정할 수 있게 하는 것이 핵심이다.
 *
 * 스프라이트는 시트에서 잘라 두고 쓰지 않던 09~20번을 쓴다. 낯선 고양이가
 * 크게 앉아 있으면 "보스가 나왔다"가 한 컷에 읽힌다.
 *
 * 보스도 그냥 `Cat`이다. 별도 타입을 만들면 타겟팅·이동·피해 판정을 전부 다시
 * 써야 하는데, 반경 하나만 추가하면 기존 전투 로직이 그대로 돈다.
 */
/**
 * 보스마다 다른 기믹 조합.
 *
 * 같은 보스가 반복되면 두 번째부터는 그냥 체력 많은 적이다. 레이드의 재미는
 * **"이 보스는 이렇게 잡는다"가 학습되는 것**이고, 그러려면 보스마다 요구하는
 * 대응이 달라야 한다.
 *
 * 축을 세 개만 둔다 — 예고 패턴 순서, 순간이동 빈도, 취약 창의 길이와 시점.
 * 축이 더 늘면 플레이어가 무엇이 다른지 못 읽는다.
 */
/**
 * 예고 패턴 차례에 들어갈 수 있는 토큰.
 *
 * `TelegraphShape`(순수 기하) + 보스 전용 파생 패턴들. `patterns`·
 * `phase2Patterns` 두 필드가 같은 유니온을 길게 반복해 적던 것을 하나로
 * 묶었다 — 새 패턴을 추가할 때 자리 하나만 고치면 된다.
 */
export type BossPattern =
  | TelegraphShape
  | "gather"
  | "stomp"
  | "hearth"
  | "quake"
  | "creep"
  | "sweep"
  | "polarity"
  | "seize";

export interface BossKit {
  /**
   * 이 보스의 강도 배수. 체력과 광역 피해에 함께 곱한다.
   *
   * 기믹이 다르면 같은 수치라도 체감이 다르다. 실측에서 무쇠발톱은 통과율
   * 100%, 살금이는 45%가 나왔다 — 순간이동이 근접의 시간을 갉아먹기 때문이다.
   * 기믹을 손대는 대신 여기서 저울을 맞춘다. 다만 **평탄하게 맞추려 하지는
   * 않는다** — 살금이는 램프에 가파르게 반응하고(6웨이브 95% → 15웨이브 45%)
   * 무쇠발톱은 완만하다(3웨이브 85% → 12웨이브 100%). 순간이동이 근접의
   * 시간을 갉아먹는 정도가 팀 크기에 따라 달라지기 때문이고, 그건 기믹의
   * 성격이지 결함이 아니다. 집계 통과율만 목표에 두고 편차는 남긴다.
   */
  readonly power: number;
  /**
   * 문턱마다 순환할 예고 패턴.
   *
   * `"gather"`는 원형이되 **안으로 들어가야 하는** 예고다. 흩어짐만 있으면
   * 예고가 뜰 때마다 그냥 누르면 되므로 판단이 없다.
   */
  /**
   * 예고 패턴 차례.
   *
   * `gather`는 안에 있어야 하고 나머지는 피해야 한다. `stomp`는 **보스 발밑**에
   * 생기는 원형이다 — 다른 넷이 전부 팀을 따라오는 것과 달리 이것만 자리가
   * 고정이라, **보스에게서 얼마나 떨어져 서느냐**가 대형의 결정이 된다.
   */
  readonly patterns: readonly BossPattern[];
  /**
   * 페이즈 2(문턱 인덱스 3부터 — battle.ts PHASE2_FROM_IDX)에서 바꿔 도는 패턴 차례. 없으면 페이즈 내내
   * `patterns`만 돈다.
   *
   * **스테이지 우두머리(`Cat.stageBoss`)에게만 적용된다.** 중간보스는 걸음
   * 하나를 채우는 자리라 성격이 끝까지 같아야 "이 보스는 이렇게 잡는다"가
   * 흔들리지 않는다. 전환의 무게는 걸음의 끝(우두머리)에만 실린다.
   *
   * `battle.ts`의 `makeTelegraph`가 `boss.thresholdIdx`를 그대로 이어서
   * 이 배열에 돌린다 — 페이즈 2 진입이 인덱스 0부터 다시 시작하지 않고
   * 문턱 순번 중간에서 이어받는다. 패턴은 순환표일 뿐 서사적 순서가 아니므로
   * 문제가 안 된다.
   */
  readonly phase2Patterns?: readonly BossPattern[];
  /** 문턱 몇 개마다 순간이동하는가. 0이면 안 한다 */
  readonly teleportEvery: number;
  /** 취약 창이 열리는 체력 비율 */
  readonly vulnerableAt: number;
  /** 취약 창 길이(ms) */
  readonly vulnerableMs: number;
}

/**
 * 보스마다 **다른 대형을 요구한다.**
 *
 * 어느 모양이 무엇을 보상하는지 패턴별로 쟀다(`npm run formation`의 패턴별
 * 노출표). 예고당 잘못 선 마리수 — 낮을수록 그 대형에 유리하다.
 *
 *   gather (움직이는 보스)  뭉침 1.00 · 정석 1.04 · 분산 2.60   → 뭉침을 보상
 *   circle                 분산 1.00 · 나머지 2.00              → 분산을 보상
 *   cone                   분산 2.00~2.82 · 나머지 4.00         → 분산을 보상
 *   stomp                  분산 2.67 · 나머지 3.00              → 분산을 약하게
 *   line                   분산 1.81 · 감싸기 3.00              → 감싸기를 벌함
 *   gather (제자리 보스)    넷 다 4.00                           → 아무것도 안 가름
 *
 * **`gather`는 보스가 움직일 때만 작동한다.** 제자리 보스는 안전지대가 늘 같은
 * 방향에 고정돼 어느 대형이든 같은 수가 밖에 남는다. 그래서 모이기는
 * 순간이동하는 보스에게 준다.
 *
 * 정석과 감싸기를 보상하는 모양은 아직 없다. 노출을 마리수로만 세면 감싸기의
 * 값(약한 것을 안쪽에 숨긴다)이 안 보이기 때문이다 — 그건 지표를 고쳐야
 * 드러난다. 지금은 뭉침과 분산 둘로만 가른다.
 */
export const BOSS_KITS: Record<number, BossKit> = {
  // 무쇠발톱 — 육중하게 **발밑을 구른다.** 발구르기·원형 위주라 답은 흩어지기.
  //   느리게 걸어오므로 거리를 벌 시간이 있다.
  //
  // 패턴 셋째 자리를 quake에서 gather로 바꿨다. 무쇠발톱은 항상 첫 보스라
  // 청록(모임)을 한 번도 못 보고 살금이(둘째 보스, id 10)에서 처음 만나면
  // "이 색은 뭐지"가 정예 난이도에서 시작된다. 첫 보스 안에서 한 번은
  // 가르쳐야 살금이가 시험이 된다. avoid 셋 · gather 하나면 정체성(발구르기·
  // 흩어지기)은 여전히 avoid가 주도한다.
  9: {
    power: 1.0,
    // 발구르기(stomp) 하나를 순차 스윕(sweep)으로 바꿨다. 무쇠발톱은 첫 보스이자
    // S2 중간보스로도 재사용되는 "가르치는" 킷이라 신규 패턴의 첫 노출 자리로
    // 맞다 — 정체성(avoid 위주·거리 유지)은 그대로고, "발밑 고정" 대신 "행이
    // 차례로 켜진다"는 새 읽기 하나만 얹는다. sweep 자체가 짧은 fuse(700ms)의
    // line avoid 연쇄라 흩어지기 정체성과 어긋나지 않는다.
    patterns: ["quake", "sweep", "gather", "cone"],
    /**
     * 페이즈 2. `thresholdIdx`가 페이즈 경계에서 리셋되지 않고 이어지므로
     * (문턱 3부터), 이 배열에서 실제로 도는 자리는 idx 3,4,5 mod 4 = 3,0,1
     * 이다(인덱스 2는 이 순환에서 한 번도 안 밟히는 죽은 자리 — 살금이
     * phase2Patterns 주석과 같은 함정). 그래서 실제 발동 순서는
     * [gather(인덱스3) → quake(인덱스0) → stomp(인덱스1)]이다.
     *
     * stomp(보스 발밑 고정)를 여기 인덱스1에 되살렸다 — 순차 스윕(sweep)이
     * 패턴 1(phase1)에서 발구르기 자리를 대신하면서 stomp가 어느 킷에서도
     * 안 뜨는 죽은 타입이 됐었다(리뷰 권고). stomp는 "보스 발밑에 서 있으면
     * 맞는다"는 앞라인 압박이라 C2(앞라인 전진/후퇴, docs/raid-design.md)의
     * 짝 기믹으로 남겨 둘 값이 있다. 페이즈 2의 **마지막**(idx5) 자리에
     * 둔 이유: 페이즈 2 진입 직후(idx3~4)는 취약 창이 막 닫힌 뒤라 근접이
     * 보스에 붙어 있을 확률이 높은데, 그 타이밍에 stomp까지 겹치면 "붙어서
     * 때리기"와 "떨어져서 피하기"가 매 문턱 충돌한다(예전에 이 이유로 아예
     * 뺐었다) — 순환 맨 끝에 두면 그 충돌 창을 피하면서도 stomp가 다시
     * 노출된다.
     */
    phase2Patterns: ["quake", "stomp", "quake", "gather"],
    teleportEvery: 2,
    vulnerableAt: 0.5,
    vulnerableMs: 3000,
  },
  // 살금이 — 자리를 옮기며 **안전한 자리를 연다.** 모이기 위주라 답은 뭉치기.
  //   순간이동이 안전지대를 매번 다른 곳에 열어 주므로 모이기가 여기서만 산다.
  // teleportEvery를 1로 뒀더니 근접이 영영 못 붙어 W10 통과율이 1.1%였다.
  // 순간이동은 "붙었다 놓쳤다"의 리듬이어야지 추격전이 되면 안 된다.
  10: {
    power: 0.85,
    patterns: ["gather", "line", "gather", "cone"],
    // 페이즈 2 — 순서를 뒤집는다(cone,gather,line,polarity). `thresholdIdx`가
    // 페이즈 경계에서 리셋되지 않고 이어지므로(문턱 3부터), 남은 세 문턱이
    // 실제로 도는 값은 [polarity, cone, gather]다 — 배열 인덱스 2(line)는
    // 이 순환에서 한 번도 안 밟히는 자리라(idx 3,4,5 mod 4 = 3,0,1) 거기 뭘
    // 둬도 안 뜬다. 원래 거기 있던 gather 하나를 극성(polarity)으로 바꿨다:
    // "모이기 위주라 답은 뭉치기"라는 정체성 위에 "판 반쪽은 모여야 하고
    // 반쪽은 흩어져야 한다"를 한 번 얹는다. resolveIntent는 아직 극성을
    // 산개로만 처리하므로(N5 설계 메모) 이 자리는 보이는 것만으로도 값이
    // 있다 — 다음 단계(C1)가 수동 선택을 붙인다.
    phase2Patterns: ["cone", "gather", "line", "polarity"],
    teleportEvery: 2,
    vulnerableAt: 0.6,
    vulnerableMs: 2600,
  },
  // 서리귀 — 제자리에서 **원형만 던진다.** 답은 흩어지기이고, 무쇠발톱과 답은
  //   같지만 창이 늦게 열리고 길다 — 오래 버티고 한 번에 몰아치는 보스.
  //   모이기를 빼는 이유는 제자리 보스에게서 그 패턴이 아무것도 안 가르기 때문이다.
  11: {
    power: 1.0,
    patterns: ["hearth", "circle", "hearth", "cone"],
    /**
     * 페이즈 2 — US-403으로 한 번 더 바꾼다. `thresholdIdx`가 페이즈 경계에서
     * 리셋되지 않고 이어지므로(문턱 3부터), 남은 세 문턱(idx 3,4,5)이 도는
     * 자리를 배열 그대로 3칸에 맞췄다 — **죽은 자리 없이 idx 3,4,5가
     * 인덱스 0,1,2에 정확히 하나씩 대응한다**(3%3=0, 4%3=1, 5%3=2). 예전
     * 4칸 배열(hearth·quake·hearth·creep)은 인덱스 2 하나가 이 순환에서
     * 한 번도 안 밟히는 죽은 자리였다(순차 스윕 phase2Patterns 주석과 같은
     * 함정) — 3칸으로 줄이면 그 함정 자체가 사라진다.
     *
     * 실제 발동 순서는 **seize(idx3) → creep(idx4) → hearth(idx5)**다.
     * seize(표식 구원, 발키르 오마주)와 creep(성장형 장판, 파멸 오마주)가
     * 나란히 서서 P2를 "파멸+발키르"의 리치왕 구도로 만든다 — 서리귀
     * 페이즈 2의 정체성이 이 둘로 옮겨간다. quake(땅울림)는 여기서 빠진다:
     * 대비축(화톳불↔땅울림) 하나보다 이 둘의 조합이 더 값을 하고, 세
     * 자리뿐인 순환에 넷을 다 욱여넣으면 다시 죽은 자리가 생긴다. quake는
     * 무쇠발톱 킷에 그대로 남아 있으므로 패턴 자체가 죽지는 않는다.
     *
     * **seize를 굳이 idx3(문턱 0.4)에 두는 이유 — idx4(0.25)에 뒀다가
     * 실측에서 거의 안 뜨는 것을 잡았다.** 서리귀의 취약 창(`vulnerableAt`
     * 0.35)이 idx4보다 먼저 열리는데, 그 창(4.5초)이 열려 있는 동안에도
     * `tickBoss`가 뒷 절반부터는 문턱을 계속 검사한다(연타로 체력이 이미
     * 훅 꺼진 상태) — 팀이 창 하나로 35%→10% 밑까지 몰아치면 idx4의 문턱
     * (0.25)과 US-404 최종 국면의 문턱(0.1)이 **같은 창 안에서 동시에
     * 만족돼**, 먼저 검사되는 최종 국면이 idx4를 통째로 건너뛴다(실측:
     * 완벽 정책 300판에서 최종 국면 도달 47.9%인데 seize 발동은 2회뿐이었다).
     * idx3(0.4)은 `vulnerableAt`(0.35)보다 항상 먼저 오므로 이 경합 자체가
     * 없다 — 창이 열리기 전에 이미 발동해 있다. creep은 idx4로 밀려 같은
     * 위험을 안지만, 이 자리는 이미 예전 버전에서도 "가끔 안 뜰 수 있는"
     * 취급이었으므로(아래 hearth와 같은 사정) 새 기믹(seize)을 안전한 자리로
     * 옮기는 쪽이 옳다 — 실측(`구원 성공률`)이 나와야 값을 잴 수 있다.
     *
     * 마지막 자리(idx5)에 hearth를 남겨 둔 이유: idx5는 `BOSS_THRESHOLDS`의
     * 마지막 문턱(0.1)과 같아 US-404 최종 국면의 발동 조건과 정확히 겹친다
     * (`battle.ts`의 `finalPhaseEligible`). 최종 국면은 문턱 로직보다 먼저
     * 가로채므로(진행 중이던 예고가 있으면 안 가로챈다) 보통은 hearth가
     * 뜨기 전에 최종 국면이 열리고, 창을 놓쳐 전투가 이어질 때만 hearth가
     * 뒤늦게 발동한다 — 어느 쪽이든 자리가 붕 뜨지 않는다.
     */
    phase2Patterns: ["seize", "creep", "hearth"],
    teleportEvery: 0,
    vulnerableAt: 0.35,
    vulnerableMs: 4500,
  },
};

export function bossKit(breedId: number): BossKit {
  if (breedId === 12) return SNIPER_KIT;
  return BOSS_KITS[breedId] ?? BOSS_KITS[9]!;
}

export const BOSS_BREEDS: readonly Breed[] = [
  {
    id: 9,
    name: "무쇠발톱",
    color: "gray",
    cls: "warrior",
    kind: "melee",
    // 실제 수치는 balance.ts의 배수로 정해진다. 여기 값은 그 배수의 기준일 뿐이다.
    hp: 120,
    atk: 14,
    atkInterval: 900,
    range: 0.8,
    // 느리게 걷는다. 보스가 빠르면 뒷줄이 순식간에 지워져 배치가 무의미해진다.
    moveSpeed: 0.45,
    manaPerAttack: 0,
    skill: null,
    passive: null,
    cost: 0,
  },
  {
    id: 10,
    name: "살금이",
    color: "black",
    cls: "rogue",
    kind: "melee",
    hp: 100,
    atk: 12,
    atkInterval: 850,
    range: 0.8,
    // 계속 순간이동하므로 걸음은 느려도 된다. 빠르면 추격전이 된다.
    moveSpeed: 0.4,
    manaPerAttack: 0,
    skill: null,
    passive: null,
    cost: 0,
  },
  {
    id: 11,
    name: "서리귀",
    color: "white",
    cls: "mage",
    kind: "ranged",
    hp: 130,
    atk: 12,
    atkInterval: 1000,
    // 제자리에서 던지는 보스라 사거리가 길다. 근접이 붙어야 할 이유가 된다.
    range: 3.2,
    moveSpeed: 0.25,
    manaPerAttack: 0,
    skill: null,
    passive: null,
    cost: 0,
  },
];

/**
 * 보스가 광역기를 쓰는 체력 문턱.
 *
 * 마나가 아니라 체력으로 발동시키는 이유: 마나는 시간에 비례해 차므로 전투가
 * 길어질수록 기계적으로 반복된다. 체력 문턱은 **진행에 묶여** 있어서 "체력이
 * 줄면 뭔가 온다"가 학습되고, 마지막 문턱이 가장 위험한 순간과 겹친다.
 * WoW 레이드가 페이즈를 체력 %로 가르는 것과 같은 이유다.
 *
 * 부수 효과 하나가 이롭다 — 화력이 약한 팀은 문턱을 천천히 넘으므로 같은
 * 시간에 더 적은 광역기를 맞는다. 어려운 판이 저절로 조금 너그러워진다.
 */
export const BOSS_THRESHOLDS = [0.85, 0.7, 0.55, 0.4, 0.25, 0.1] as const;

/** 예고가 뜨고 터질 때까지. 반응할 시간을 주되 늘어지지 않는 길이. */
export const TELEGRAPH_FUSE_MS = 1200;

/**
 * 순차 스윕(sweep) 한 파동의 도화선.
 *
 * **파동 단위(리뷰 (ㄷ), C3)로 바뀌면서 700ms(행 하나짜리 점멸)를 버렸다.**
 * 행 0→4를 순차로 점멸시키던 예전 설계는 문턱 하나가 최대 5번의 개별
 * 회피를 요구했는데, 이 게임의 공용 자원(`ACT_COOLDOWN_MS` 1초·
 * `dodgeCharges` 보스전당 2회)으로는 완벽하게 반응해도 그 빈도를 못
 * 따라갔다(SWEEP_DMG_MUL 주석 참고). 홀수 행 묶음 → 짝수 행 묶음, 두
 * 파동으로 바꾸면 문턱 하나가 요구하는 개입이 최대 2회로 줄고, 도화선을
 * 쿨다운(1초)보다 넉넉히 길게 잡아야 "한 파동을 피한 뒤 다음 파동도 받을
 * 여유"가 생긴다 — 1200ms(기본 예고)보다도 조금 더 여유를 둔다.
 */
export const SWEEP_FUSE_MS = 1400;

/**
 * 성장형 장판(creep)의 반경 성장 단계(칸). 첫 값이 스폰 시 반경이자 원래
 * "circle avoid" 경고 반경이기도 하다(makeTelegraph가 그대로 쓴다).
 */
export const CREEP_RADIUS_STEPS = [0.9, 1.5, 2.2, 3.0] as const;

/** 성장형 장판이 성장/피해를 재는 틱 간격(ms). */
export const CREEP_TICK_MS = 700;

/** 성장형 장판이 아무도 없는 채로 이만큼 지나면 소멸한다(ms). */
export const CREEP_IDLE_DESPAWN_MS = 1400;

/**
 * 성장형 장판의 틱당 고정 피해 배수(보스 공격력 기준).
 *
 * 예고 회피 실패(telegraphHit)는 최대체력 비율이라 사실상 즉사에 가깝다
 * (balance.ts의 telegraphDmg=1.277). creep은 "소량 고정 피해"가 설계 —
 * 반경이 자라며 벗어나기 어려워지는 것 자체가 압박이지, 틱 하나하나가
 * 치명적이면 회피할 필요 없이 그냥 죽는 장판이 된다. 보스 공격력의 절반을
 * 고정값으로 쓴다 — 웨이브 램프를 따라 같이 커지되(공격력이 이미 램프를
 * 탄다) 평타 한 대보다는 가볍다.
 */
export const CREEP_DMG_MUL = 0.5;

/**
 * 순차 스윕(sweep) 한 파동의 피해 배수. `telegraphHit`(battle.ts)이 재는
 * `frac`에 곱한다.
 *
 * **1.0(원래 avoid 배율) — 피해를 눌러서 자원 문제를 가리지 않는다.**
 * 예전엔 행 0→4 순차(700ms 간격 5회)가 공용 자원(쿨다운 1초·차지 2회)을
 * 못 따라가서 이 배율을 0.25까지 낮춰 "맞아도 소량"으로 눌렀는데, 그건
 * 진짜 문제(자원 예산과 안 맞는 빈도)를 가리는 것이었다 — 실제로 0.15까지
 * 더 낮춰도 sim 중앙값이 11에서 안 움직였다(막다른 길이라는 증거). 진짜
 * 수정은 빈도 쪽이다 — `SWEEP_FUSE_MS`가 파동 단위로 바뀌어(리뷰 (ㄷ))
 * 문턱 하나가 요구하는 개입이 최대 2회로 줄고 도화선도 쿨다운보다 넉넉해
 * 졌으므로, **제대로 피하면 이 배율이 얼마든 안 맞는다** — 다른 avoid
 * 패턴(quake·cone 등)과 같은 배율로 되돌려 "피해야 할 이유"를 정직하게
 * 남긴다.
 */
export const SWEEP_DMG_MUL = 1.0;

/**
 * 순차 스윕(sweep) 문턱 하나를 이루는 파동들 — 각 원소가 그 파동에서 동시에
 * 켜지는 행 번호 배열이다(battle.ts `enqueueSweep`이 그대로 큐에 싣는다).
 *
 * **둘째 파동을 [0,2,4](3행)에서 [0,4](2행)로 줄였다 — US-401, 행 수 축이
 * 주 손잡이였다.** sweep 도입 뒤 비개입 첫 보스 통과율이 83.1%→40.8%로
 * 떨어졌는데(리뷰 실측), `SWEEP_DMG_MUL`을 아무리 눌러도 안 움직인다는
 * 기록이 이미 있었다(위 주석의 0.15 실측). 문턱 하나가 요구하는 파동은
 * 그대로 둘인데, 둘째 파동이 5행 중 3행을 막아 "안전지대"가 좁았던 것이
 * 진짜 원인이었다 — 첫째 파동(홀수 1·3)을 피해 짝수 행으로 옮겨도 둘째
 * 파동이 그 짝수 행 셋을 전부 다시 막으니 몸을 또 옮겨야 했다.
 *
 * `bestFreeCell`(run.ts)이 새 고양이를 중앙 행(2)에 가깝게 앉히는 편향이
 * 있어서(뒷줄 다음으로 mid까지 거리로 정렬) **어느 행을 막는지가 대칭이
 * 아니다.** 실측(npm run intervention, 300시드, 첫 보스만):
 *
 *   구성                          비개입   완벽    sim중앙값
 *   [1,3]→[0,2,4] mul1.0(원래)     40.8%   87.8%      13
 *   [1,3]→[2]     mul1.0           43.3%   90.2%      15   (중앙 행만 막아도 별 차이 없음 — 대부분이 거기 몰려 있어서)
 *   [1,3]→[0,4]   mul1.0           69.2%   93.9%      14   ← 채택
 *   [1,3]→[0,4]   mul0.7           77.1%   94.6%      14
 *   [1,3]→[0,4]   mul0.6           79.1%   94.6%      14
 *   [1,3]→[0,4]   mul0.5           80.1%   94.6%      14
 *
 * `[2]`(중앙 하나만)는 오히려 `[0,2,4]`보다 별로 안 나아진다 — 신규
 * 고양이가 기본으로 중앙에 몰리므로 중앙 행 하나가 가장 붐빈다. `[0,4]`
 * (가장자리 둘)가 진짜 빈 자리를 막으므로 같은 2행이라도 효과가 다르다.
 * mul을 0.5~0.7로 더 낮춰도 비개입이 몇 %p 더 오르는 것 말고는(완벽·
 * 중앙값 둘 다 그대로) 목표(비개입 60%↑·완벽 92%↑·중앙값 13~15)를 이미
 * mul 1.0에서 다 채우므로, `SWEEP_DMG_MUL` 주석의 원칙("피해야 할 이유를
 * 정직하게 남긴다")을 그대로 지켜 배율은 손대지 않는다 — 행 수만으로 푼다.
 *
 * **상시 안전행이 있다 — 의도된 트레이드오프다.** 두 파동([1,3]·[0,4])을
 * 합쳐도 행 2(가운데)는 한 번도 안 막힌다 — 실측(`w3-sweep.mjs`)으로 팀의
 * 48.9%가 파동이 뜨는 순간 이미 행 2에 있어 아무 데도 안 움직인다. US-401은
 * "문턱 하나가 요구하는 회피를 최대 2회로 줄인다"가 목표였지 "안전지대를
 * 없앤다"가 아니었다 — 원래(행 0→4 순차)도 회피 후 다음 파동이 겹치지 않는
 * 자리는 늘 있었다. 상시 안전행을 없애려면(예: [1,3]→[2,4] 같은 조합) 파동
 * 하나가 요구하는 이동 폭이 커져 SWEEP_DMG_MUL·차지 예산을 다시 맞춰야 하는
 * 별도 작업이다 — 지금은 US-401 목표(행 수 축)가 이미 관문을 채웠으므로
 * 손대지 않는다.
 */
export const SWEEP_WAVE_ROWS: readonly (readonly number[])[] = [
  [1, 3],
  [0, 4],
];

/**
 * 표식(seize, US-403) 도화선. 다른 예고(`TELEGRAPH_FUSE_MS` 1200ms)보다
 * 조금 길게 잡는다 — 표식이 걸린 아군 한 마리가 구원 원까지 걸어가거나
 * 옆의 아군에게 붙을 시간이 필요하고, 이건 몸을 "지금 서 있던 자리에서
 * 빼는" 것이 아니라 "다른 자리로 데려가는" 것이라 더 걸린다.
 */
export const SEIZE_FUSE_MS = 1500;

/** 구원 원(gather)의 반경(칸). 극성(polarity)의 원(1.3)보다 한 칸 여유를 더 준다 —
 * 표식냥 하나만 정확히 노려야 하는 극성과 달리 여기는 팀 전체가 몰려도 된다. */
export const SEIZE_GATHER_RADIUS = 1.5;

/**
 * "인접"의 기준 거리(칸). 표식냥이 구원 원 대신 **아군 누군가와 이 거리
 * 안**이면 구원된다 — 뭉친 대형이면 굳이 원까지 안 가도 산다는 뜻이다.
 *
 * **1.3에서 0.9로 내렸다.** 1.3은 몸이 안 겹치는 평소 대형 간격(전투 로직의
 * `SEPARATION` 1.0)보다 커서, 실측(`w3-lab.mjs`)으로 표식이 걸리는 순간
 * 이미 97.5%가 "요구 0"(아무것도 안 눌러도 구원)이었다 — 표식이 판단이
 * 아니라 장식이 된 것이다. `SEPARATION`(1.0) **아래**로 내리면 평소
 * 밀어내기 간격만으로는 자동 구원이 안 되고, 실제로 붙이는(겹치다시피
 * 다가서는) 움직임이 있어야 한다. 인접 경로 자체를 없애는 것은 이번엔
 * 안 한다 — 0.9 적용 뒤에도 요구 0 비율이 90%를 넘으면(재측정 필요) 다음
 * 웨이브에서 인접 경로 폐지를 검토할 것.
 */
export const SEIZE_ADJACENT_RADIUS = 0.9;

/**
 * 구원 실패 시 표식냥이 받는 피해 — **최대 체력 비율**. 즉사가 아니라 크게
 * 아픈 정도로 잡는다(권장값 55%). 예고 회피 실패(`telegraphHit`)처럼 사실상
 * 즉사에 가까운 값을 쓰면 "표식은 곧 죽음"이 되어 구원이라는 판단 자체가
 * 의미를 잃는다 — 놓쳐도 되돌릴 수 있어야 다음 표식에서 다시 시도할 이유가 있다.
 */
export const SEIZE_FAIL_FRAC = 0.55;

/** 구원 실패 시 넉백 거리(칸). 보스 반대 방향으로 밀려나 "튕겨 나갔다"가 눈에 보인다. */
export const SEIZE_KNOCKBACK = 1.4;

/**
 * 최종 국면(finalPhase, US-404) 채널 길이(ms). 전능 채널 — 예고도 공격도
 * 멈춘다. 2.5초는 "화면이 어두워지고 다시 밝아진다"는 연출 한 호흡이 읽히기에
 * 충분하면서, 아무 입력도 못 받는 시간을 과하게 늘리지 않는 길이다.
 */
export const FINAL_CHANNEL_MS = 2500;

/**
 * 최종 국면이 채널 뒤에 여는 마지막 취약 창(ms). 서리귀의 평소 취약 창
 * (`vulnerableMs` 4500)보다 조금 길게 잡는다 — 회복 직후 다시 대형을 잡고
 * 몰아치기까지의 시간을 더 준다. 새 타이머가 아니라 기존 `Cat.vulnerableMs`를
 * 그대로 빌려 쓴다(`battle.ts`) — 창이 열리고 닫히는 로직·버튼 문구
 * ("할퀴기!")·연타 판정을 전부 공짜로 재사용한다.
 */
export const FINAL_VULNERABLE_MS = 5000;

/** 최종 국면 채널이 끝나며 전 아군이 되찾는 체력 — 최대 체력 비율. */
export const FINAL_HEAL_FRAC = 0.6;

/**
 * 마지막 취약 창이 열릴 때 보스 체력의 바닥 — 최대 체력 비율.
 *
 * 발동 문턱은 10%지만, 그 순간 실제 hp는 훨씬 낮게(때로는 1~2%) 잡히는
 * 판이 흔하다 — 문턱을 검사하는 시점과 실제로 걸리는 시점 사이에 이미
 * 여러 타가 들어가 있어서다. 그러면 입력이 하나도 없어도 창이 1.4초 만에
 * 끝나(실측) "마지막 창"이라는 이름값을 못 한다. 채널이 끝나는 순간 hp를
 * 이 값 **아래로만** 끌어올린다(`Math.max`) — 이미 이보다 많이 남아 있으면
 * 그대로 둔다.
 */
export const FINAL_MIN_HP_FRAC = 0.08;

/**
 * 보스가 순간이동으로 옮겨 앉는 자리(적 진영 기준 열, 행).
 *
 * 문턱을 넘을 때마다가 아니라 **한 번 걸러** 뛴다. 매번 뛰면 근접이 영원히
 * 쫓아만 다니게 되어 전투가 추격전이 된다. 걸러 뛰면 "붙었다 → 놓쳤다 →
 * 다시 붙는다"의 리듬이 생기고, 그 사이에 예고가 낀다.
 *
 * 자리를 순환시키는 이유는 예고의 기준점이 매번 달라져야 배치가 한 번 정하고
 * 끝나는 것이 아니게 되기 때문이다.
 *
 * 가장자리 행(0, 4)은 쓸 수 없다. 보스는 반경 1.5칸이라 행 0에 앉으면 몸의
 * 절반이 판을 넘어 HUD를 덮는다(브라우저에서 실제로 그랬다 — 머리가 생선
 * 카운터 위로 올라왔다). 예고는 `drawTelegraphs`가 판으로 잘라 주지만 보스
 * 스프라이트는 잘리지 않으므로, 자리 자체를 안쪽으로 들인다. 행 1~3이면
 * 반경 1.5칸이 격자 안에 정확히 들어간다.
 */
export const BOSS_ANCHORS = [
  { col: 2, row: 2 },
  { col: 1, row: 1 },
  { col: 3, row: 3 },
  { col: 1, row: 3 },
  { col: 3, row: 1 },
] as const;

/** 보스가 차지하는 반경(칸). 일반 고양이는 0이라 기존 계산이 그대로다. */
export const BOSS_RADIUS = 1.5;

/**
 * 첫 보스(5웨이브)는 반드시 무쇠발톱이다.
 *
 * `floor(wave/5)`를 그대로 쓰면 5웨이브에 두 번째 보스가 나온다. 실제로 그렇게
 * 두었더니 첫 보스가 순간이동을 매 문턱마다 하는 살금이가 되어 비개입 통과율이
 * 41%로 떨어졌다. 첫 보스는 기믹을 **가르치는** 자리다 — 세 패턴을 다 보여주고
 * 창을 한 번 크게 여는 교과서가 먼저 와야 한다.
 */
/**
 * @param index 이번이 몇 번째 보스인가 (0부터). 웨이브 번호로 계산하면 주기를
 *   바꿀 때마다 어긋난다 — 실제로 floor(wave/5)를 쓰다가 첫 보스에 두 번째
 *   보스가 나온 적이 있다.
 */
/**
 * 저격수 — 저격 웨이브의 미니 보스.
 *
 * 저격 웨이브는 통과율로는 두 번째 벽이었는데(88%) 플레이어가 할 수 있는 게
 * 없었다. 원거리가 많아 일방적으로 맞는 구간일 뿐이었다.
 *
 * 저격수를 세우면 그 웨이브가 **보스전의 예고편**이 된다 — 직선 예고 하나만
 * 쓰고, 회피 차지도 준다. 3웨이브 뒤에 올 보스에서 쓸 동작을 낮은 대가로
 * 먼저 가르치는 자리다.
 */
export const SNIPER_BREED: Breed = {
  id: 12,
  name: "외눈이",
  color: "orange",
  cls: "archer",
  kind: "ranged",
  hp: 60,
  atk: 10,
  atkInterval: 1100,
  // 보스보다 사거리가 길다. 뒤에서 쏘는 역할이 분명해야 한다.
  range: 3.6,
  moveSpeed: 0.3,
  manaPerAttack: 0,
  skill: null,
  passive: null,
  cost: 0,
};

/** 저격수의 반경. 보스(1.5)보다 작아 미니 보스로 읽힌다. */
export const SNIPER_RADIUS = 0.85;

/**
 * 저격수는 직선만 쏜다. 규칙이 하나여야 예고편 노릇을 한다.
 *
 * power가 1.8인 이유: 1.0에서는 예고가 떠도 회피 유무로 통과율이 0.1%p밖에
 * 안 갈렸다. 아무 일도 안 일어나면 가르치는 게 아니다. 1.8에서 저격 웨이브가
 * 90.3%(회피 안 함) 대 92.4%(회피 함)로 갈린다 — 고양이 하나를 잃는 정도의
 * 대가라 벽은 아니지만 버튼을 누를 이유는 된다.
 */
export const SNIPER_KIT: BossKit = {
  power: 1.8,
  patterns: ["line"],
  teleportEvery: 0,
  vulnerableAt: 0,
  vulnerableMs: 0,
};

export function bossForIndex(index: number): Breed {
  return BOSS_BREEDS[index % BOSS_BREEDS.length] ?? BOSS_BREEDS[0]!;
}
