# AGENTS.md

이 저장소에서 코딩 에이전트(Codex / Claude Code 등)가 지켜야 할 규칙.
사람이 읽어도 되는 요약은 `README.md`에 있다.

## 프로젝트 한 줄

브라우저에서 도는 픽셀 고양이 오토배틀러. 5×5 보드에 고양이를 배치하고 자동
전투를 관전하며 몇 웨이브까지 버티는지 겨룬다. 매 판의 시너지 규칙은 LLM이
빌드타임에 생성하고 검증기를 통과한 것만 게임에 들어간다.

**한 걸음의 순서는 `map` → `reward` → `prepare` → `battle`이다.** 길을 골라야
상대가 정해지고(`chooseNode`가 `buildEnemyWave`와 `rollOffers`를 부른다), 그
상대를 보면서 산 다음, 산 것을 배치한다. 이 순서를 바꾸면 측정 스크립트 열 개가
전부 옛 게임을 재게 된다 — 국면 전이는 `run.ts`에만 두고 브라우저와 하네스가
같은 함수(`chooseNode` · `leaveShop`)를 부른다.

## 절대 규칙

1. **런타임 네트워크 호출 금지.** 배포된 게임은 외부로 어떤 요청도 보내지 않는다.
   LLM은 `scripts/gen-synergies.mjs`에서 빌드타임에만 쓰고, 결과는
   `src/data/synergies.json`으로 커밋한다. 심사자에게 API 키·결제·로그인을
   요구하면 두 공모전 모두 실격이다.
2. **LLM 출력은 신뢰하지 않는다.** 모든 생성 결과는
   `src/validate/synergy-schema.ts`의 화이트리스트를 통과해야 하고, 수치는
   `EFFECT_RANGE`로 하드 클램프된다. 이 계약을 우회하는 코드는 넣지 않는다.
3. **밸런스는 눈대중으로 바꾸지 않는다.** `src/game/balance.ts`의 값을 만졌으면
   반드시 `npm run sim`을 돌려 도달 웨이브 분포를 확인한다. 목표는 중앙값
   10~15웨이브에 무한 런 0건.
4. **의존성을 늘리지 않는다.** 현재 런타임 의존성은 0개다. 게임 엔진, 상태
   관리 라이브러리, UI 프레임워크를 도입하지 않는다.
5. **커밋을 squash하지 않는다.** 커밋 히스토리 자체가 공모전 제출물이다.

## 명령

| 명령 | 하는 일 |
|------|---------|
| `npm run dev` | 개발 서버 |
| `npm run build` | 타입체크 + 프로덕션 빌드 (`dist/`) |
| `npm test` | 검증기 적대 테스트 — 규칙 위반 입력이 실제로 막히는지 |
| `npm run sim` | 밸런스 시뮬레이션 300런, 도달 웨이브 분포 |
| `npm run sweep` | 튜너블 조합 스윕 |
| `npm run slice` | 캐릭터 시트 → 개별 스프라이트 (Python + Pillow 필요) |
| `npm run gen:synergies` | 시너지 규칙 생성 → `src/data/synergies.json` |
| `npm run probe` | 기기 10종 레이아웃 겹침 검사 |
| `npm run docs` | `docs/*.md` → PDF |

## 구조

```
src/game/types.ts      도메인 타입, 보드 좌표 헬퍼, 전투 좌표계
src/game/skills.ts     능력 8종(액티브 6·패시브 2)의 판정. 연출은 battle.ts가 맡는다
src/game/theme.ts      색 토큰, 픽셀 숫자 글리프, 텍스트 헬퍼
src/game/balance.ts    밸런스 튜너블 (여기만 만지면 된다)
src/game/breeds.ts     고양이 8종 정의. color는 시너지 판정 기준이라 외형과 일치해야 함
src/game/run.ts        런 상태, 웨이브 생성, 상점, 시너지 적용
src/game/battle.ts     고정 스텝 전투 시뮬레이션
src/game/layout.ts     레이아웃 계산 (렌더와 입력이 공유). 판은 늘 세로, 넓으면 양옆 세로줄
src/game/render.ts     캔버스 렌더링
src/game/icons.ts      아이콘(생선·지도 칸). 전부 Canvas 2D 패스 — 외부 요청 0건이라 폰트도 SVG도 못 쓴다
src/game/sprites.ts    스프라이트 로딩
src/game/audio.ts      음악. 게임 로직은 이걸 모른다 — main.ts가 상태를 보고 튼다
src/validate/          LLM 출력 검증기 — 빌드타임과 런타임이 공유
scripts/               슬라이싱·생성·테스트·시뮬레이션
```

## 주의할 함정 (실제로 밟았던 것들)

- **"분포가 같으니 UI만 바뀐 것이다"는 증거가 아니다.** 하네스가 그 파일을 안
  읽으면 **지워도 같은 숫자가 나온다.** `scripts/` 중 `main.ts`를 임포트하는 것은
  하나도 없고 `render.ts`는 `layout-probe`뿐이다 — 즉 그 둘을 `throw`로 갈아
  치워도 `npm run sim`은 그대로 통과한다. 실제로 구매·배치 병합 커밋이 그 논법을
  썼다. 결론은 맞았지만(그 커밋의 `run.ts` 델타가 `state.notice` 한 줄이고 어떤
  하네스도 그걸 안 읽는다) **인용한 근거가 결론을 지지하지 않았다.**
  로직이 안 바뀌었음을 보이려면 **무엇이 바뀌었고 그것을 누가 읽는가**를 대야 한다.

- **하네스가 구매 정책 사본을 갖게 두지 마라.** `bot-policy.mjs`의 `shopStep`
  하나만 쓴다. 사본은 조용히 갈라지고, 갈라지면 **축 값이 틀린 채로 초록이 된다.**
  하루에 네 번 밟았다 — `balance-sim`↔`metrics-gen`(재추첨 리셋 시점이 달라 같은
  코드에서 p25가 8과 9), `map-space`(유료 재추첨을 안 써서 지도 격차가 1.4로
  부풀었다, 실제 0.7), `relic-space`(무료 재추첨을 놓쳐 유물 격차가 4.3으로
  부풀었다, 실제 3.9). **버그를 고칠 때마다 축 값이 내려갔다** — 사본은 늘
  후하게 재는 쪽으로 틀린다.

- **새 판정 지표는 귀무모형에 먼저 건다**(`npm run null`). 지도 축 판정을 신탁
  기준으로 바꿔 관문을 초록으로 만든 적이 있는데, **전략적 차이가 정의상 0인
  비교가 더 잘 통과했다**(같은 정책·다른 시드로 최다 35.7%·이득 4.48 vs 진짜
  38.7%·3.59). 도달 웨이브 편차가 크고 `rng`가 런당 한 줄기라 선택이 난수 소비
  횟수를 바꾸는 순간 그 뒤가 다른 런이 된다 — 같은 시드의 두 정책은 짝이 아니라
  독립 표본이고, "셋 중 최대"를 "하나의 평균"과 견주면 전략과 무관하게 늘 몇
  웨이브가 남는다. 평균 비교는 이 함정이 없다.

- **판정이 기준에 걸쳐 있으면 잡음 바닥부터 잰다.** 같은 코드를 겹치지 않는
  시드 블록 셋에 돌려 보는 것으로 충분하다. 결정 축 셋이 전부 기준 여유보다
  잡음이 컸다 — 지도 1.5/0.7/0.8(기준 1.5) · 배치 0.8/1.2/0.6(기준 1.5) ·
  유물 2.5/3.4/2.8(기준 4.0). **시드 블록 하나만 보고 "지도 축이 살아났다"고
  보고할 뻔했다.** 세 스크립트는 이제 판정 줄 아래에 자기 밴드를 함께 찍는다.
  과거 유물 보너스 배율 스윕(1.15~1.45)이 3.2~4.0을 무작위로 오간 것도 같은
  이유였다 — 그 폭 전체가 잡음 안이었다.

- **분산은 판수보다 난수 구조로 줄이는 게 싸다.** 지도 생성은 전역 스트림이
  아니라 `makeRng(mixSeed(runSeed, stage))`로 뽑는다. 전역 스트림은 전투
  회피 판정이 공격 횟수만큼 먹으므로, 지도를 거기서 뽑으면 "1스테이지에서 몇
  대 때렸나"가 2스테이지 지도를 바꾼다. 분리 후 **같은 시드는 정책과 무관하게
  같은 지도를 낸다(60시드 281개 스테이지에서 일치율 100%).** 새 난수 소비처를
  만들 때, 그것이 정책 비교에 쓰이는 것이면 같은 방식으로 갈래를 나눌 것.


- **레벨 성장은 지수여야 한다.** 적은 웨이브마다 복리로 크는데 플레이어만
  선형으로 크면 반드시 따라잡힌다. 선형 버전은 웨이브 2에서 전멸했다.
- **강화 비용도 지수여야 한다.** 효과는 지수인데 비용이 선형이면 후반에 무한히
  강해진다. 선형 비용 버전은 시뮬 12%가 상한(60웨이브)에 걸려 런이 안 끝났다.
- **살 수 없는 오퍼는 목록에서 걷어내야 한다.** 보드가 꽉 찬 상태에서 영입
  카드를 남겨두면 눌러도 아무 일이 안 일어나고, 자동 플레이는 무한 루프에 빠진다.
- **전투 타임아웃은 패배로 처리한다.** 남은 체력 비율로 판정하면 탱키·회피 조합이
  아무도 죽이지 않고 매 웨이브 타임아웃 승리를 가져가며 런이 끝나지 않는다.
- **스프라이트 배경 제거는 flood fill로만.** 전역 색상 치환을 쓰면 크림색(#18)과
  흰색(#05·#14) 고양이의 몸통까지 지워진다.
- **상대 임포트에 `.ts` 확장자를 붙인다.** Node ESM 하네스(`sim`/`test`)가 같은
  모듈 그래프를 쓰기 위해 필요하다.
- **전투 좌표는 화면과 분리한다.** 유닛은 `types.ts`의 정준 좌표(fx, fy)에서 움직이고,
  `layout.ts`의 `fieldToScreen`이 화면으로 옮긴다. 전투 계산에 화면 크기나 방향이
  섞이면 헤드리스 시뮬과 실제 게임이 갈라진다.
- **세로 화면에서 보드는 전치된다.** fx가 "적 쪽" 축이라 세로에서는 열이 세로로
  늘어선다. 이렇게 하지 않으면 "적에게 가까운 열이 앞줄"이 세로에서 깨지고,
  유닛이 옆으로 붙어야 하는데 위로 걸어가는 그림이 나온다.
- **근접이 한 덩어리로 뭉치면** `SEPARATION`(겹침 방지)과 `CROWD_PENALTY`(타겟 분산)를
  본다. 둘 다 없으면 전원이 같은 적에게 달려들어 한 점에 쌓인다.
- **보드 칸 수는 제한이 아니다.** 5x5는 25칸이라 칸으로는 아무것도 못 막는다.
  진짜 제한은 `unitCap(wave)`다. 영입·적 생성 모두 이걸 본다.
- **새 고양이는 `bestFreeCell`로 놓는다.** 빈 칸 앞 인덱스에 놓으면 5x5에서는
  뒷줄부터 채워져 근접이 여섯 칸을 걸어간다. 측정 결과 중앙값이 8에서 13으로 갈렸다.
- **전사는 맞을 때도 마나가 찬다.** 없으면 전사 스킬이 원거리의 3~6분의 1밖에
  안 나간다(4번 때려야 하는데 걸어가는 시간까지 있어서). TFT가 탱커에게 피격
  마나를 주는 것과 같은 이유다.
- **시너지 칩은 폭에 따라 배치가 달라진다.** 좁으면 두 줄로 접고 장식인 이름을
  버린다. 안 그러면 세로 화면에서 글자가 겹친다.
- **카드 높이를 보드 예산에 넣지 마라.** 카드를 세우려면 240px쯤 필요한데 그만큼
  보드에서 떼면 데스크톱에서 셀이 60px에서 30px로 반토막 난다. 대신 `offerCards`가
  `offers` 띠에서 **위로만** 자란다.
  **불변식이 뒤집혔다.** 전에는 "덮어도 된다 — 그동안 보드가 입력을 안 받으니까"
  였고, 그 근거가 `canRearrange()`가 `prepare`에서만 참이라는 것이었다. 구매와
  배치를 한 화면으로 합치면서 보상 단계에서도 보드를 만지므로 그 근거가 사라졌다.
  지금 계약은 **세로줄 구성(데스크톱)에서 카드가 보드를 0% 덮을 것**이고
  `npm run probe`가 검사한다. 접힌 구성(폰)은 둘을 다 띄우면 셀이 손가락으로 집을
  수 없어져 덮는 것을 허용하되, **탭은 카드 영역이 전부 삼킨다**(`main.ts`의
  보상 분기가 빈 슬롯과 카드 사이 틈까지 막는다). 안 막으면 보이지도 않는 보드의
  고양이가 잡힌다 — 실제로 그랬다.
  HUD와 안내 문구는 어느 구성에서도 절대 덮지 않는다(무엇을 살 수 있는지 모르고
  사게 된다).
- **`unitCapBase`는 `starterCount`보다 커야 한다.** 같으면 시작부터 만석이라 영입
  카드가 아예 안 나오고 교체·강화만 뜬다 — 시작 상점을 열어 놓고 정작 새 고양이를
  못 사는 상태가 된다. 지금은 3마리로 시작해 한도 4다.
- **보이는 곳과 눌리는 곳은 한 함수에서 나와야 한다.** 카드 사각형은 렌더도
  히트테스트도 `offerRects(L)` 하나만 쓴다. 둘을 따로 계산하면 반드시 갈라진다.
- **알려진 빨간불은 `verify` 체인의 맨 뒤에 둔다.** `verify`는 `&&`로 이어져 있어
  중간에서 exit 1이 나면 **그 뒤는 영영 안 돈다.** `npm run map`이 체인 중간에
  있던 동안 `gold`·`build`·`metrics` 셋이 한 번도 실행되지 않았고, 그래서 기준
  수치가 손으로 따로 돌려야만 갱신됐다. AGENTS가 적어 둔 "관문 안에 있던 것만
  살아 있었다"를 관문 자신이 재현한 셈이다. 새로 미달인 시험을 추가할 때도
  같은 자리(맨 뒤)에 둘 것.

## 코드 스타일

- 주석은 한글. "무엇"이 아니라 **"왜"**를 쓴다. 특히 위 함정들처럼 되돌리기 쉬운
  결정에는 이유를 남긴다.
- 커밋 메시지는 Conventional Commits, description은 한글:
  `feat(battle): 근접·원거리 이동 전투 구현`
