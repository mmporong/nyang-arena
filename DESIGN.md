# 냥 아레나 — 제출 버전 디자인 계약

- 상태: Active
- 최종 갱신: 2026-08-24
- 적용 범위: OpenAI Game Builder Challenge Track 1 제출 버전
- 한 줄 정의: **고양이 부대를 만들고, Codex가 제안한 검증 가능한 ‘악몽 계약’을 골라 다음 보스의 규칙과 보상을 스스로 정하는 브라우저 레이드 오토배틀러**

이 문서는 제출 버전의 제품·게임·UI 결정을 한곳에 고정한다. 과거 설계의 의도와
실험 기록은 `docs/design-brief.md`, `docs/raid-design.md`, `docs/readability-study.md`,
`docs/art-direction.md`에 남아 있지만, 충돌할 때는 이 문서가 우선한다.

`docs/art-brief.md` §3의 “밸런스·전투 로직 불가침”은 트랙 A 시각 작업을 위한
변경 격리 규칙이었다. 이후 사용자가 해커톤 적합성을 위해 게임의 방향과 근간 변경을
명시적으로 허용했으므로, 제출 버전의 악몽 계약·준비 경로에는 그 제한을 적용하지
않는다. 대신 6축과 metrics를 새 기준으로 전부 재검증하며 기존 작업을 Codex 작업으로
재표기하지 않는다. 런타임 의존성 0·외부 요청 0·접근성·권리 제약은 계속 유지한다.

## 1. Source of truth

### 공식 평가와 제출 조건

공식 사이트와 약관에 공개된 다섯 평가축은 Playability, Originality,
Codex Collaboration, Release Potential, Presentation이다. 수치 배점이나 가중치는
공개되지 않았다. 제출물에는 제목, 200자 이내 소개, 공개 브라우저 링크, 16:9 권장
썸네일이 필요하다. 명시된 가점 자료는 두 가지다.

1. 실제 플레이를 포함한 3분 이내 데모 영상
2. Codex를 어디에, 어떤 기능으로, 어떤 문제 해결에 썼고 사람이 무엇을 결정했는지
   설명한 기록

기존 프로젝트도 허용되지만 행사 이전과 행사 중 개선 범위를 명확히 구분해야 한다.
따라서 기존 레이드·상점·배치 기반은 선행 프로토타입으로 공개하고, 이번 제출 버전의
신규 핵심 루프·데이터 검증·테스트·발표 패키지를 Codex 기여로 증명한다.

### 코드의 기준

- 게임 상태 전이는 `src/game/run.ts`가 소유한다.
- 브라우저와 헤드리스 측정은 같은 게임 함수를 호출한다.
- 보스의 실행 가능한 패턴 토큰은 `src/game/bosses.ts`가 소유한다.
- 생성 후보는 신뢰하지 않는다. `src/validate/raid-contract-schema.ts`의 검증을 통과한
  계약만 `src/data/raid-contracts.json`에 들어간다.
- 밸런스 판단은 화면 인상보다 반복 시뮬레이션을 우선한다.

## 2. Brand

### 약속

“귀여운 고양이”가 입구이고, **내가 고른 악몽의 규칙을 읽고 깨는 레이드**가 기억에
남아야 한다. AI는 장식 문구가 아니라 플레이 가능한 규칙 후보를 만들고, 검증기는
불공정하거나 실행 불가능한 후보를 막는다.

### 성격

- 따뜻한 한밤의 고양이 다방과 위협적인 악몽의 대비
- 짧고 자신감 있는 한국어
- 결과보다 선택과 학습을 보여 주는 태도
- “AI가 다 했다”가 아니라 “AI 제안을 검증 가능한 게임 규칙으로 바꿨다”는 정직성

## 3. Product goals

### 핵심 목표

1. 첫 60초 안에 지도 선택, 구매, 배치, 전투를 한 번 경험한다.
2. **첫 화면에서** 첫 보스 계약 세 장을 읽고 하나를 선택한다. 선택 뒤에 지도가
   열리므로 심사위원은 30초 안에 제출 버전의 차별점을 본다.
3. 계약 카드에서 **규칙·대응·위험도·추가 보상**을 5초 안에 비교한다.
4. 선택한 계약이 실제 보스 패턴과 처치 보상에 그대로 반영된다.
5. 위험도에 따라 낮음→일반 전투, 중간→정예, 높음→정찰이라는 준비 경로를
   제시하고, 계약 직후 지도 관문에서 세 경로를 모두 열어 이중 고리로 표시한다.
6. 같은 시드는 같은 계약 제안을 만들며 `?raid=<code>` 주소로 다른 기기에서도
   같은 첫 보스 계약을 재현할 수 있다.
7. 제출 영상 3분 안에 선택→대비→전투→부검→Codex 검증 증거를 모두 보여 준다.

### 공식 평가축에 대한 제품 답변

| 평가축 | 제출 버전의 증거 |
|---|---|
| Playability | 첫 화면 계약, 위험별 준비 경로, 10기기 레이아웃, 동일 함수 기반 시뮬레이션, 공개 Pages 빌드 |
| Originality | 플레이어가 다음 보스의 규칙과 보상 위험을 직접 조합하는 악몽 계약 루프 |
| Codex Collaboration | 후보 JSON, 적대 후보, 생성 스크립트, 런타임 검증기, 채택 결과, 커밋별 검증 로그 |
| Release Potential | 계약 팩을 데이터로 추가할 수 있고 같은 시드/공유 코드로 비동기 도전을 확장 가능 |
| Presentation | 3분 영상 흐름, 계약 카드 한 장의 명확한 전후 관계, 결과 화면의 계약 부검 |

### 비목표

- 런타임 LLM/API 호출
- 로그인, 서버 리더보드, 과금
- 새 캐릭터 스프라이트나 새 런타임 의존성
- 읽기 어려운 자연어를 임의 해석하는 범용 규칙 생성기
- 기존 Claude 작업을 Codex 작업으로 재표기하는 것

## 4. Personas and jobs

### 처음 온 심사위원

- 30초 안에 무엇을 고르고 왜 고르는지 알고 싶다.
- 별도 설명 없이 실제 플레이가 되는지 확인하고 싶다.
- Codex가 게임 화면과 규칙에 무엇을 바꿨는지 한 장에서 보고 싶다.

### 전략 플레이어

- 다음 보스 규칙을 보고 팀·배치·자원 사용을 바꾸고 싶다.
- 높은 보상을 위해 더 어려운 계약을 일부러 고르고 싶다.
- 같은 조건을 다시 해 보거나 다른 사람에게 공유하고 싶다.

### 개발 과정 평가자

- 생성 결과가 그대로 제품에 들어가지 않았다는 증거가 필요하다.
- 재현 가능한 테스트, 불변식, 지표 변화와 인간 결정의 경계를 확인하고 싶다.

## 5. Information architecture

```text
첫 실행 / 새 런
  → 첫 보스 악몽 계약 3개 비교·선택
지도
  ├─ 계약 직후 낮음=일반 / 중간=정예 / 높음=정찰 준비 관문
  ├─ 세 경로가 같은 확인 전투로 합류 → 상점·배치 → 전투
  └─ 보스 선택
       → 다음 보스 미리보기 + 상점·배치
       → 계약이 바꾼 보스전
       → 보상 → 다음 계약 3개 비교·선택
게임 오버
  ├─ 막힌 보스·예고 대응·선택 계약 부검
  └─ 같은 시드 / 오늘의 시드 / 도전
```

별도 페이지나 새 상태 기계를 만들지 않는다. `map` 국면 안에서 계약 오버레이를
연다. 첫 런은 지도보다 먼저 첫 보스 계약을 고르고, 이후에는 보스를 쓰러뜨린 직후
다음 보스 계약을 고른다. 선택한 계약은 그 보스에 도달할 때까지 유지된다. 브라우저와
측정 하네스가 같은 전이를 쓴다.

## 6. Design principles

1. **선택 전에 결과를 말한다.** 규칙·대응·위험·보상을 계약 카드에서 모두 보여 준다.
2. **AI 제안과 출고 규칙을 구분한다.** 후보, 거절 사유, 채택 결과를 재현 가능하게 둔다.
3. **한 번에 새 문법 하나.** 계약은 기존 보스 패턴 토큰을 재조합하고 새 전투 조작을
   동시에 요구하지 않는다.
4. **색만으로 말하지 않는다.** 위험도는 발톱 수와 텍스트, 대응은 동사와 패턴 아이콘을
   함께 쓴다.
5. **같은 시드는 같은 약속이다.** 제안 순서·보스·전투·공유 코드가 재현되어야 한다.
   `?seed=<0..4294967295>`는 선택 전 세 장을 재현하는 데모 진입점이다.
   계약을 고르면 현재 주소를 `?raid=NA1-<seed36>-<contract-id>`로 바꾸고, 그 주소를
   열면 같은 시드와 같은 첫 계약이 자동 적용된다. 잘못된 코드는 안전하게 새 런으로
   폴백한다.
6. **보상은 유혹이지 정답이 아니다.** 위험이 높은 계약일수록 보상이 크되 어느 계약도
   무조건 우월하지 않게 시뮬레이션한다.
7. **위험을 지도 행동으로 번역한다.** 낮음은 평범한 전투의 생선, 중간은 정예의
   보장 유물, 높음은 정찰의 추가 회피를 택한다. 계약 직후 세 경로는 항상 모두
   열리고 다음 확인 전투에서 합류하므로 이후 경로 운이 준비 효과를 오염시키지 않는다.
   추천 경로 밖을 고를 자유는 남기되 계약 보스 체력·광역 피해가 3% 강해진다.
8. **유물은 추첨이 아니라 보스 승리의 드래프트다.** 완료한 보스 이벤트가 별도
   드래프트를 열고, 서로 다른 유물 세 장 중 하나를 고르거나 건너뛴 뒤 일반 상점으로
   이어진다. wave 수나 준비 경로에 묶지 않아 전투·정예·정찰이 같은 기회를 얻는다.

## 7. Visual language

기존 “한밤의 고양이 다방 결투장” 팔레트를 유지한다. 새 계약 UI는 종이 계약서와
붉은 봉인을 모티프로 하되, 전투 신호의 고채도 색을 빼앗지 않는다.

- 배경: `T.inkDeep`, `T.floor`
- 카드: 따뜻한 짙은 갈색 면 + `T.paper` 테두리
- 선택/봉인: 저채도 테라코타, 활성 CTA만 `T.action`
- 위험도: `발톱 1~3` 도형 + “낮음/중간/높음” 텍스트
- 대응: “흩어져라 / 모여라 / 자리를 바꿔라 / 표식을 구하라”처럼 동사 우선
- 보상: 생선 아이콘과 `+숫자`, 화폐색 `T.fish`
- 선택된 계약: 보스 HUD 아래 작은 봉인 칩으로 계속 노출

## 8. Components

### Contract overlay

- 제목: “악몽과 계약할 시간”
- 부제: “다음 보스의 규칙과 추가 보상을 하나 고르세요”
- 1~3번 카드와 키보드 단축키를 같은 순서로 배치한다.
- 카드 전체가 클릭 영역이다. 선택 후 확인 단계를 추가하지 않는다.
- 좁은 세로 화면에서는 세로 스택, 넓은 화면에서는 3열이다.

### Contract card

필수 정보 순서:

1. 계약명
2. 위험도(발톱 수 + 텍스트)
3. 규칙: 보스가 무엇을 다르게 하는가
4. 대응: 플레이어가 무엇을 바꿔야 하는가
5. 보상: 승리 시 추가 생선
6. 선택 키 `1/2/3`

카드 본문은 최대 2줄씩, 12px 미만으로 축소하지 않는다. 계약명은 7글자, 규칙과
대응은 각각 공백 포함 34자 이내로 제한한다. 공유 코드는 선택 전 카드에서 빼고,
선택 뒤 활성 계약 칩과 주소에만 보여 준다. 카드별 장식 차이는 허용하지만 정보
순서는 바꾸지 않는다.

### Active contract chip

- 상점·배치: 다음 보스 이름과 계약명을 함께 보여 대비 근거로 쓴다.
- 전투: 보스 HUD 아래 계약명과 핵심 대응 한 줄만 남긴다.
- 게임 오버: 마지막 보스 계약과 성공/실패 맥락을 부검에 포함한다.
- 선택 직후: 주소에 공유 가능한 `?raid=` 코드가 반영되며 칩에 짧은 코드를 표시한다.
- 계약 직후 지도: 위험별 추천 노드를 금색 이중 고리와 텍스트로 함께 표시한다.

### Codex proof panel

제품 런타임 안에 개발자 대시보드를 억지로 넣지 않는다. 제출 영상과 문서에서 다음
파일을 나란히 보여 준다.

- `scripts/raid-contract-candidates.json`: Codex 제안 원본
- `scripts/raid-contract-adversarial.json`: 범위 초과·금지 토큰·중복 등 적대 입력
- `scripts/gen-raid-contracts.mjs`: 검증 후 출고 데이터 생성
- `src/data/raid-contracts.json`: 실제 게임이 읽는 채택 결과
- 검증 로그: 몇 개가 왜 거절되고 무엇이 클램프됐는지

## 9. Accessibility

- 포인터와 키보드 `1/2/3`으로 동일하게 계약을 선택한다.
- 위험도·대응·보상을 색 하나로 구분하지 않는다.
- 모든 핵심 텍스트는 시스템 한글 폰트와 충분한 대비를 사용한다.
- `prefers-reduced-motion`에서는 계약 카드 등장 이동을 생략한다.
- 숨겨진 탭 복귀, 반복 키 입력, 계약 중 Enter 입력이 상태를 건너뛰지 않게 한다.
- 최소 터치 영역 44×44px, 계약 카드는 그보다 크게 유지한다.
- 작은 화면에서도 계약 본문을 자르거나 가로 압축하지 않는다.

## 10. Responsive behavior

- 900px 이상: 중앙 3열 카드, 각 카드 폭은 전체의 약 28%, 카드 간격 3%.
- 600~899px: 가로면 3열, 세로면 1열 스택. 횡스크롤은 쓰지 않는다.
- 599px 이하 또는 세로형: 3개 카드를 세로로 쌓고 화면 높이에 맞춰 조밀하게 배치한다.
- 900×320 극저높이: 제목·부제를 한 줄로 합치고 카드 설명을 각 1줄로 줄이되 규칙,
  대응, 보상은 모두 남긴다.
- 기존 10기기 `layout-probe`를 통과해야 하며 계약 카드 겹침도 같은 검사에 포함한다.

## 11. Interaction states

| 상태 | 표시 | 입력 |
|---|---|---|
| 보스 노드 선택 전 | 기존 지도와 보스 힌트 | 노드 클릭 또는 숫자 |
| 계약 제안 열림 | 3개 카드, 다른 지도 입력 차단 | 카드 클릭 또는 1/2/3 |
| 계약 선택 직후 | 선택 봉인 피드백 후 기존 보상 화면 | 중복 선택 무시 |
| 보스 보상 | 유물 3장 택1·건너뛰기 → 일반 상점 | 카드 클릭 또는 건너뛰기 |
| 상점·배치 | 다음 보스 + 선택 계약 칩 | 기존 구매·배치 |
| 계약 보스전 | 보스 HUD + 핵심 대응 | 기존 Space/G |
| 승리 | 기본 보상 + 계약 추가 보상 명시 | 다음 단계 |
| 패배 | 계약명과 실패 맥락 부검 | 재도전 선택 |
| 데이터 오류 | 안전한 기본 계약으로 폴백, 디버그 로그 | 플레이 차단 금지 |

## 12. Content and voice

- 제목은 2~7글자 명사구: “갈라진 밤”, “번지는 어둠”, “무너지는 바닥”.
- 규칙은 보스 주어: “보스가 판을 양쪽 규칙으로 가릅니다.”
- 대응은 명령형: “빛나는 쪽을 읽고 산개/집결을 직접 고르세요.”
- 위험은 형용사 하나: 낮음, 중간, 높음.
- 보상은 수치로: “승리 시 생선 +4”.
- 실패 메시지는 비난하지 않고 다음 시도를 가르친다.
- 제출 설명은 기여 경계를 명시한다:
  “기존 프로토타입을 바탕으로, 챌린지 제출 버전의 핵심 차별화 루프인 악몽 계약
  시스템은 Codex가 분석·설계·구현·검증했습니다.”

### 3분 데모 고정 동선

- 기준 시드: `424242` (구현 후 첫 계약 제안과 패턴 노출을 검증해 최종 확정)
- 선택 계약: “갈라진 밤”
- 0:00~0:12 — 첫 화면의 계약 세 장과 선택 이유
- 0:12~0:38 — 계약 직후 위험 ‘중간’ 이중 고리인 가위눌림 경로와 보장 유물 선택
- 0:38~1:05 — 배치와 첫 일반 전투는 2~4배속 편집
- 1:05~1:50 — 계약 보스 실제 속도, 극성의 산개/집결 수동 선택
- 1:50~2:10 — 계약 보너스 또는 실패 부검, 주소의 공유 코드
- 2:10~2:35 — Codex 후보에서 불법 토큰이 거절되고 채택 JSON이 만들어지는 과정
- 2:35~3:00 — 6축·불변식·10기기·공개 URL 검증 결과와 기여 경계

컷 편집은 지도·상점·일반 전투에만 허용하고, 계약 선택과 계약 보스의 첫 패턴부터
판정까지는 실제 속도를 유지한다.

## 13. Implementation constraints

### 하드 제약

- 런타임 외부 네트워크 요청 0건
- 런타임 의존성 0개 유지
- 기존 Canvas 2D, 세로·가로 단일 코드 경로 유지
- 검증하지 않은 생성 데이터 런타임 유입 금지
- 허용 패턴 토큰 외 값 거절, 수치 범위 클램프, id 중복 거절
- 계약 선택 전 적 웨이브 생성 금지
- 계약 추가 보상은 승리할 때만 지급
- 같은 `(seed, stage, boss ordinal)`은 같은 3개 제안과 같은 순서를 생성
- 일반/정예/정찰의 기존 상태 전이와 보스 외 전투 규칙 보존
- 준비 경로는 기존 노드 보상을 재사용한다. 낮음은 일반 전투, 중간은 정예 보장
  유물, 높음은 정찰 회피 2회 + 계약 대비 1회다. 정찰 회피는 고위험 계약의
  추천 경로에서만 지급하고 여러 정찰은 중첩하지 않는다. 추천 밖의 경로를 고르면
  입력 자원을 빼앗지 않고 대상 계약 보스의 체력·광역 피해만 1.03배로 보정한다.

### RaidContract 실행 스키마

```ts
interface RaidContract {
  id: string;                    // `[a-z][a-z0-9_]{2,23}`, 중복 거절
  name: string;                  // 2~7글자, 꺾쇠 제거
  risk: 1 | 2 | 3;               // 카드 발톱 수와 위험 텍스트
  rule: string;                  // 보스가 바꾸는 것, 최대 34자
  counter: string;               // 플레이어 대응, 최대 34자
  rewardFish: number;            // 정수 2~6, 범위 밖은 클램프
  patterns: BossPattern[];       // 3~4개, 허용 토큰 외 값이면 계약 전체 거절
  phase2Patterns?: BossPattern[];// 3~4개, 스테이지 우두머리 후반부에만 사용
  proofCode: string;             // 출고 데이터 추적용 고정 식별자
}
```

- 선택 계약의 `patterns`는 대상 보스의 기본 `patterns`를 **교체**한다. 새 기하나
  런타임 코드는 만들지 않고 기존 토큰의 실행 경로를 재사용한다.
- `phase2Patterns`가 있으면 스테이지 우두머리의 기본 후반 배열을 교체하고, 없으면
  계약의 `patterns`를 후반에도 쓴다.
- 순간이동 주기와 취약 시점·길이는 원래 보스 정체성을 보존한다. 위험 1은 원래
  `power`, 위험 2는 `×1.12`, 위험 3은 `×1.50`을 체력·광역 피해에 함께 적용해
  추가 보상과 실제 난도를 맞바꾼다.
- 보상은 대상 보스를 이긴 직후 기본 보스 보너스에 더하고, 패배·일반전에 지급하지 않는다.
- 계약은 선택 당시 계산한 `targetBossIndex`와 실제 보스 인덱스가 같을 때만 적용한다.
- 출고 JSON은 정확히 6개가 모두 유효할 때만 원자 채택한다. 실패하면 서로 다른
  안전 계약 3장으로 전체 폴백하고, 상태·거절 사유를 console/debug에 남긴다.
- 공유 코드가 유효하지 않으면 새 런으로 폴백하고 주소의 잘못된 값을 제거한다.

### 완료 관문

1. `typecheck`, 계약 적대 테스트, 기존 검증기 테스트 PASS
2. `probe`, 오디오, 에셋, 회피, 불변식, 빌드 PASS
3. 6축 측정 결과를 변경 전·후로 기록하고 의도치 않은 퇴행 설명 0건
4. 홀드아웃 시드 `10001..11800`에서 계약 인지 정책이 보상 숫자만 보고 기존
   안전 경로·기존 보스 대형을 쓰는 정책보다 생존 중앙값 **+1.0악몽 깊이 이상**
   또는 첫 두 보스 통과율 **+5%p 이상**이어야 한다. 두 정책 모두 전투 중 공개
   예고에는 반응하므로 일부러 못 싸우는 기준선을 만들지 않는다.
5. 출고 계약 6개를 각각 홀드아웃의 모든 시드에서 첫 두 보스에 연속 적용해 독립 실행한다. 계약별 평균 깊이
   최대-최소는 **4.0 이하**, 1위의 95% 구간은 2위와 겹쳐야 하며, 각 계약의 첫 두
   보스 통과율은 **70~97%** 안에 있어야 한다. 경로 선택이 전투 RNG 소비를 바꾸므로
   동일 시드 순위나 제안 노출률로 나눈 승점은 지배도 근거로 사용하지 않는다.
6. 같은 시드 반복 실행의 계약 제안·최종 요약 해시 일치
7. 데스크톱·모바일·900×320에서 포인터/키보드 계약 선택 실동작
8. 공개 Pages에서 새 계약 루프와 번들 해시 확인
9. 커밋마다 실행 명령, 종료 코드, 시드 범위, 핵심 지표를 증거 문서에 기록
10. 정적 카피 검사(이름 7자, 규칙/대응 34자), 10기기 12px 글자 하한, 900×320
    겹침 0을 통과하고 실제 브라우저에서 첫 화면·키보드·포인터를 한 번 확인
11. `docs/codex-contribution.md`에서 `문제/요청 → Codex 산출물 → 거절·채택 판단 →
    커밋 → 검증 → 사람이 결정한 것`을 한 표로 연결
12. 지도 1500런에서 정책 평균 효과가 귀무 잡음보다 큰 1.0웨이브 이상이고,
    읽는 정책은 무작위 이상·고정 최선과 0.5웨이브 이내이며 실제 준비 3경로의
    접근·보상 전달은 300시드 전부 성공
13. 유물 드래프트는 보스 승리 이벤트마다 중복 없는 3장·택1을 열고, 선택·건너뛰기
    모두 같은 보상 국면의 일반 카드 3장으로 이어져야 한다. 전투·정예·정찰 준비
    경로별 노출이 같아야 한다. 전투 전용 지도에서 2000시드로 직업 집중 정책과
    동일 정책의 무유물 대조군을 함께 재며,
    최선 집중 빌드는 일반 무유물 기준보다 **4.0 이상**, 집중 빌드 최저 이득은
    **1.0 이상**, 상위 두 직업 격차는 **1.5 이하**여야 한다. 유물만 켜고 끈
    한계효과는 평균 **+0.5 이상**, 최저 **-0.25 이상**, 5직업 중 3직업 이상이
    **+0.5 이상**이어야 한다. 모든 유물 카드는 조건·정확한 이득·대가를 표시한다.

## 14. Resolved decisions

- 계약 추가 보상은 위험별 2~6 생선이며 위험 1은 2로 통일했다.
- 기본 헤드리스 정책은 낮은 위험, 계약 공간 상한 정책은 로스터·패턴을 읽는다.
- 공유는 `?raid=` 자동 로드를 출고 범위로 삼고 별도 클립보드 권한은 요구하지 않는다.
- 고정 영상은 `?seed=424242`에서 세 장을 보여 준 뒤 선택 순간 `?raid=`로 전환한다.
- 별도 훈련 모드는 만들지 않고 첫 계약 카드와 지도 이중 고리로 30초 안에 규칙을 가르친다.
