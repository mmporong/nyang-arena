# 냥 아레나 — 제품 디자인 계약

- 상태: Active
- 최종 갱신: 2026-08-25
- 적용 범위: 게임 런타임·UI/UX·접근성의 제품 기준
- 한 줄 정의: **고양이 부대를 만들고, Codex가 제안한 검증 가능한 ‘악몽 계약’을 골라 다음 보스의 규칙과 보상을 스스로 정하는 브라우저 레이드 오토배틀러**

현재 자동 개선 작업은 게임 자체만 다룬다. 수동 플레이 절차와 제출 문안·영상·사이트는
작업열에서 제외한다. 아래 제출 관련 기록은 기존 제품 방향의 근거로만 보존하며 이번
UI 구현의 완료 조건으로 사용하지 않는다.

이 문서는 제출 버전의 제품·게임·UI 결정을 한곳에 고정한다. 과거 설계의 의도와
실험 기록은 `docs/design-brief.md`, `docs/raid-design.md`, `docs/readability-study.md`,
`docs/art-direction.md`에 남아 있지만, 충돌할 때는 이 문서가 우선한다.

`docs/art-brief.md` §3의 “밸런스·전투 로직 불가침”은 트랙 A 시각 작업을 위한
변경 격리 규칙이었다. 이후 사용자가 해커톤 적합성을 위해 게임의 방향과 근간 변경을
명시적으로 허용했으므로, 제출 버전의 악몽 계약·준비 경로에는 그 제한을 적용하지
않는다. 대신 6축과 metrics를 새 기준으로 전부 재검증하며 기존 작업을 Codex 작업으로
재표기하지 않는다. 런타임 의존성 0·외부 요청 0·접근성·권리 제약은 계속 유지한다.

## 1. Source of truth

### 공식 평가와 제출 조건

공식 사이트와 약관에 공개된 다섯 평가축은 Playability, Originality,
Codex Collaboration, Release Potential, Presentation이다. 수치 배점이나 가중치는
공개되지 않았다. 제출물에는 제목, 200자 이내 소개, 공개 브라우저 링크, 16:9 권장
썸네일이 필요하다. 명시된 가점 자료는 두 가지다.

1. 실제 플레이를 포함한 3분 이내 데모 영상
2. Codex를 어디에, 어떤 기능으로, 어떤 문제 해결에 썼고 사람이 무엇을 결정했는지
   설명한 기록

기존 프로젝트도 허용되지만 행사 이전과 행사 중 개선 범위를 명확히 구분해야 한다.
따라서 기존 레이드·상점·배치 기반은 선행 프로토타입으로 공개하고, 이번 제출 버전의
신규 핵심 루프·데이터 검증·테스트·발표 패키지를 Codex 기여로 증명한다.

### 코드의 기준

- 게임 상태 전이는 `src/game/run.ts`가 소유한다.
- 브라우저와 헤드리스 측정은 같은 게임 함수를 호출한다.
- 보스의 실행 가능한 패턴 토큰은 `src/game/bosses.ts`가 소유한다.
- 생성 후보는 신뢰하지 않는다. `src/validate/raid-contract-schema.ts`의 검증을 통과한
  계약만 `src/data/raid-contracts.json`에 들어간다.
- 밸런스 판단은 화면 인상보다 반복 시뮬레이션을 우선한다.

## 2. Brand

### 약속

“귀여운 고양이”가 입구이고, **내가 고른 악몽의 규칙을 읽고 깨는 레이드**가 기억에
남아야 한다. AI는 장식 문구가 아니라 플레이 가능한 규칙 후보를 만들고, 검증기는
불공정하거나 실행 불가능한 후보를 막는다.

### 성격

- 따뜻한 한밤의 고양이 다방과 위협적인 악몽의 대비
- 짧고 자신감 있는 한국어
- 결과보다 선택과 학습을 보여 주는 태도
- “AI가 다 했다”가 아니라 “AI 제안을 검증 가능한 게임 규칙으로 바꿨다”는 정직성

## 3. Product goals

### 핵심 목표

1. 첫 60초 안에 지도 선택, 구매, 배치, 전투를 한 번 경험한다.
2. **첫 화면에서** 첫 보스 계약 세 장을 읽고 하나를 선택한다. 선택 뒤에 지도가
   열리므로 심사위원은 30초 안에 제출 버전의 차별점을 본다.
3. 계약 카드에서 **규칙·대응·위험도·추가 보상**을 5초 안에 비교한다.
4. 선택한 계약이 실제 보스 패턴과 처치 보상에 그대로 반영된다.
5. 위험도에 따라 낮음→일반 전투, 중간→정예, 높음→정찰이라는 준비 경로를
   제시하고, 계약 직후 지도 관문에서 세 경로를 모두 열어 이중 고리로 표시한다.
6. 같은 시드는 같은 계약 제안을 만들며 `?raid=<code>` 주소로 다른 기기에서도
   같은 첫 보스 계약을 재현할 수 있다.
7. 제출 영상 3분 안에 선택→대비→전투→부검→Codex 검증 증거를 모두 보여 준다.

### 공식 평가축에 대한 제품 답변

| 평가축 | 제출 버전의 증거 |
|---|---|
| Playability | 첫 화면 계약, 위험별 준비 경로, 16기기 레이아웃, 동일 함수 기반 시뮬레이션, 공개 Pages 빌드 |
| Originality | 플레이어가 다음 보스의 규칙과 보상 위험을 직접 조합하는 악몽 계약 루프 |
| Codex Collaboration | 후보 JSON, 적대 후보, 생성 스크립트, 런타임 검증기, 채택 결과, 커밋별 검증 로그 |
| Release Potential | 계약 팩을 데이터로 추가할 수 있고 같은 시드/공유 코드로 비동기 도전을 확장 가능 |
| Presentation | 3분 영상 흐름, 계약 카드 한 장의 명확한 전후 관계, 결과 화면의 계약 부검 |

### 비목표

- 런타임 LLM/API 호출
- 로그인, 서버 리더보드, 과금
- 새 캐릭터 스프라이트나 새 런타임 의존성
- 읽기 어려운 자연어를 임의 해석하는 범용 규칙 생성기
- 기존 Claude 작업을 Codex 작업으로 재표기하는 것

## 4. Personas and jobs

### 처음 온 심사위원

- 30초 안에 무엇을 고르고 왜 고르는지 알고 싶다.
- 별도 설명 없이 실제 플레이가 되는지 확인하고 싶다.
- Codex가 게임 화면과 규칙에 무엇을 바꿨는지 한 장에서 보고 싶다.

### 전략 플레이어

- 다음 보스 규칙을 보고 팀·배치·자원 사용을 바꾸고 싶다.
- 높은 보상을 위해 더 어려운 계약을 일부러 고르고 싶다.
- 같은 조건을 다시 해 보거나 다른 사람에게 공유하고 싶다.

### 개발 과정 평가자

- 생성 결과가 그대로 제품에 들어가지 않았다는 증거가 필요하다.
- 재현 가능한 테스트, 불변식, 지표 변화와 인간 결정의 경계를 확인하고 싶다.

## 5. Information architecture

```text
첫 실행 / 새 런
  → 첫 보스 악몽 계약 3개 비교·선택
지도
  ├─ 계약 직후 낮음=일반 / 중간=정예 / 높음=정찰 준비 관문
  ├─ 세 경로가 같은 확인 전투로 합류 → 상점·배치 → 전투
  └─ 보스 선택
       → 다음 보스 미리보기 + 상점·배치
       → 계약이 바꾼 보스전
       → 보상 → 다음 계약 3개 비교·선택
게임 오버
  ├─ 막힌 보스·예고 대응·선택 계약 부검
  └─ 같은 시드 / 오늘의 시드 / 도전
```

별도 페이지나 새 상태 기계를 만들지 않는다. `map` 국면 안에서 계약 오버레이를
연다. 첫 런은 지도보다 먼저 첫 보스 계약을 고르고, 이후에는 보스를 쓰러뜨린 직후
다음 보스 계약을 고른다. 선택한 계약은 그 보스에 도달할 때까지 유지된다. 브라우저와
측정 하네스가 같은 전이를 쓴다.

## 6. Design principles

1. **선택 전에 결과를 말한다.** 규칙·대응·위험·보상을 계약 카드에서 모두 보여 준다.
2. **AI 제안과 출고 규칙을 구분한다.** 후보, 거절 사유, 채택 결과를 재현 가능하게 둔다.
3. **한 번에 새 문법 하나.** 계약은 기존 보스 패턴 토큰을 재조합하고 새 전투 조작을
   동시에 요구하지 않는다.
4. **색만으로 말하지 않는다.** 위험도는 발톱 수와 텍스트, 대응은 동사와 패턴 아이콘을
   함께 쓴다.
5. **같은 시드는 같은 약속이다.** 제안 순서·보스·전투·공유 코드가 재현되어야 한다.
   `?seed=<0..4294967295>`는 선택 전 세 장을 재현하는 데모 진입점이다.
   계약을 고르면 현재 주소를 `?raid=NA1-<seed36>-<contract-id>`로 바꾸고, 그 주소를
   열면 같은 시드와 같은 첫 계약이 자동 적용된다. 잘못된 코드는 안전하게 새 런으로
   폴백한다.
6. **보상은 유혹이지 정답이 아니다.** 위험이 높은 계약일수록 보상이 크되 어느 계약도
   무조건 우월하지 않게 시뮬레이션한다.
7. **위험을 지도 행동으로 번역한다.** 낮음은 평범한 전투의 생선, 중간은 정예의
   보장 유물, 높음은 정찰의 추가 회피를 택한다. 계약 직후 세 경로는 항상 모두
   열리고 다음 확인 전투에서 합류하므로 이후 경로 운이 준비 효과를 오염시키지 않는다.
   추천 경로 밖을 고를 자유는 남기되 계약 보스 체력·광역 피해가 3% 강해진다.
8. **유물은 추첨이 아니라 보스 승리의 드래프트다.** 완료한 보스 이벤트가 별도
   드래프트를 열고, 서로 다른 유물 세 장 중 하나를 고르거나 건너뛴 뒤 일반 상점으로
   이어진다. wave 수나 준비 경로에 묶지 않아 전투·정예·정찰이 같은 기회를 얻는다.

## 7. Visual language

기존 “한밤의 고양이 다방 결투장” 팔레트를 유지한다. 새 계약 UI는 종이 계약서와
붉은 봉인을 모티프로 하되, 전투 신호의 고채도 색을 빼앗지 않는다.

- 배경: `T.inkDeep`, `T.floor`
- 카드: 따뜻한 짙은 갈색 면 + `T.paper` 테두리
- 선택/봉인: 저채도 테라코타, 활성 CTA만 `T.action`
- 위험도: `발톱 1~3` 도형 + “낮음/중간/높음” 텍스트
- 대응: “흩어져라 / 모여라 / 자리를 바꿔라 / 표식을 구하라”처럼 동사 우선
- 보상: 생선 아이콘과 `+숫자`, 화폐색 `T.fish`
- 선택된 계약: 보스 HUD 아래 작은 봉인 칩으로 계속 노출

### Spacing rhythm

모든 화면은 4px 기준 간격 토큰 하나를 공유한다. 작은 화면이라고 간격을 비례 축소해
선택지가 한 덩어리처럼 보이게 하지 않는다.

| 토큰 | 값 | 용도 |
|---|---:|---|
| `space-1` | 4px | 아이콘과 숫자, 세그먼트 내부선 |
| `space-2` | 8px | 같은 행 안의 요소 |
| `space-3` | 12px | 모바일 카드·버튼 사이 |
| `space-4` | 16px | 카드 내부 패딩, 행동 영역 경계 |
| `space-5` | 24px | 데스크톱 카드·서로 다른 섹션 사이 |
| `space-6` | 32px | 제목과 핵심 콘텐츠 |

- 바깥 안전 여백은 극저높이 화면에서 8px, 그 외에는 12px 이상을 목표로 한다.
- 보드와 주 행동 버튼 사이에는 12px(극저높이) 또는 16px 이상의 빈 공간과
  명시적인 구분선을 둔다.
- 카드 내부는 `제목 → 본문 → 보상/행동` 세 구역으로 나누고 본문과 하단 구역
  사이에 1px 구분선을 둔다.
- 같은 역할의 형제 컨트롤은 모바일 8~12px, 데스크톱 12~24px 간격을 쓴다.

### Button hierarchy

- 한 화면의 주황색 `T.action` 주 CTA는 하나만 둔다.
- 보조 선택지는 짙은 갈색 면, 종이색 테두리와 글자를 사용해 주 CTA와 구분한다.
- 극성의 `산개/집결`은 두 독립 CTA가 아니라 한 세그먼트 그룹으로 보이며 2px
  내부선으로만 나눈다.
- 비활성 버튼은 맥동과 하이라이트를 제거하고 남은 시간이나 잠긴 이유를 글로 남긴다.
- 키보드 포커스는 금색 3px 외곽선과 2px 이상의 간격으로 표시한다.

## 8. Components

### Contract overlay

- 제목: “악몽과 계약할 시간”
- 부제: “다음 보스의 규칙과 추가 보상을 하나 고르세요”
- 1~3번 카드와 키보드 단축키를 같은 순서로 배치한다.
- 카드 전체가 클릭 영역이다. 선택 후 확인 단계를 추가하지 않는다.
- 좁은 세로 화면에서는 세로 스택, 넓은 화면에서는 3열이다.

### Contract card

필수 정보 순서:

1. 계약명
2. 위험도(발톱 수 + 텍스트)
3. 규칙: 보스가 무엇을 다르게 하는가
4. 대응: 플레이어가 무엇을 바꿔야 하는가
5. 보상: 승리 시 추가 생선
6. 선택 키 `1/2/3`

보통 폭의 카드는 규칙·대응을 각각 최대 2줄로 보여 준다. 남은 화면 높이에 맞춰
본문 줄 수를 늘리지 않고 카드 자체를 콘텐츠 높이에 맞춘다. 본문은 12px, 제목은
16px 미만으로 축소하지 않는다. 계약명은 7글자, 규칙과 대응은 각각 공백 포함
34자 이내로 제한한다.
공유 코드는 선택 전 카드에서 빼고, 선택 뒤 활성 계약 칩과 주소에만 보여 준다.
카드별 장식 차이는 허용하지만 정보 순서는 바꾸지 않는다.

- 모바일 카드 간격은 12px, 데스크톱은 20~24px이다.
- 3열 컨테이너는 최대 1308px, 카드 한 장은 최대 420×360px이다. 높이 620px 이상의
  데스크톱 카드는 320~360px 안에서 끝내며 가장 큰 연속 빈 높이는 24px을 넘기지 않는다.
- 카드 내부 패딩은 보통 모바일 12px, 데스크톱 16px이며 화면 높이 380px 미만,
  카드 높이 140px 미만 또는 폭 180px 미만인 압축 카드만 8px을 쓴다.
- 카드는 `번호·제목·위험 봉인 / 위험 트랙 / 보스 변화 / 내 대응 / 보상·선택`의
  다섯 밴드로 나눈다. 접두어를 본문 문장에 붙이지 않고 라벨과 문장을 별도 행으로
  분리해 스캔 순서를 고정한다.
- 위험 봉인은 발톱 개수와 3분할 고리, 텍스트를 함께 쓴다. 고위험 카드 전체를
  고채도 색으로 채우지 않고 봉인과 왼쪽 얇은 rail만 위험색을 쓴다.
- 보상은 하단 고정 구역에 20~24px 생선과 큰 `+N`으로 두고 본문과 낮은 대비의
  구분선으로 나눈다. `승리 시`는 보조 라벨로 둔다.
- 카드 전체 클릭을 유지하면서 하단에 `1/2/3 선택` 힌트를 함께 표시한다.
- 카드 폭 180px 미만에서도 계약명을 숫자로 대체하지 않는다. 번호와 계약명 앞부분을
  함께 남기고 footer 키 힌트를 생략하며 보상을 `생선 +N`으로 축약한다.
- 번호·최대 7자 제목·위험 봉인 사이에 각각 12px을 확보하지 못하면 header를
  `번호+제목 / 위험` 두 행으로 바꾼다.
- 높이 540px 이하의 짧은 세로 화면은 하단 입력 안내 영역을 접고 화면 끝까지 쓰는
  가로형 계약 티켓 3장을 세로로 쌓는다. `320×480`, `390×500`에서도 세 계약명이
  모두 보여야 하며 카드 전체 클릭과 숫자키 입력은 그대로 유지한다.
- 보통 폭 카드의 `보스 변화`에는 첫 실행 패턴을 32~40px Canvas 기믹 인장으로
  함께 표시한다. 인장은 기존 `BossPattern` 토큰의 원·선·부채꼴·균열·집결 도형을
  재사용하고 텍스트를 대체하지 않는다. 압축 티켓에서는 본문 폭을 위해 숨긴다.
- 계약 제안 중 하단 주 CTA 자리는 비활성 버튼으로 그리지 않는다. 카드가 곧 행동
  대상이며 하단에는 `카드를 누르거나 1 · 2 · 3` 입력 안내만 둔다.

### Interaction feedback

- 계약 선택은 상태와 지도를 즉시 반영하되, 선택 카드의 **자리만** 렌더 전용
  snapshot으로 최대 360ms 기억해 금색 outline→위험 발톱 도장→종이 파편과
  계약명·보상 수치를 남긴다. 이전 카드 면이나 다른 선택지는 남기지 않아 화면은
  계약인데 입력은 지도에 들어가는 거짓 상태를 만들지 않는다.
- 구매 성공은 기존 카드→보드 비행에 도착 고리, 짧은 광선, `구매 완료` 또는
  `강화 완료` 라벨을 함께 보여 준다. 유물처럼 보드 칸이 없는 구매는 보드 중앙에
  봉인한다. 실패 전이에는 성공 소리나 파티클을 재생하지 않는다.
- 재추첨 성공은 상태와 새 카드 세 장을 즉시 반영하고, 최대 300ms 동안 카드마다
  짧게 엇갈린 금색 테두리 sweep와 `새 제안` 봉인을 남긴다. 실패하면 기존 안내를
  유지하면서 카드 구역에 호박색 정적 테두리만 남기고 성공음·파티클은 내지 않는다.
- 지도 선택은 경로 전이를 즉시 적용하되, 선택한 노드의 정규화된 화면 위치와 이름만
  최대 320ms 기억해 새 화면 위에 경로 고리와 `경로 확정` 봉인을 남긴다. 이전 지도나
  다른 노드 면을 다시 그리지 않으며, 열리지 않은 길에는 확인음과 성공 연출을 내지 않는다.
- 패배 화면의 같은 판·오늘·도전 선택도 다음 런을 즉시 시작한다. 이전 버튼 면은
  남기지 않고 선택 이름만 새 화면 위에 최대 320ms 봉인한다. 전용 재진입 확인음을
  사용하며 기존 타이틀 스팅을 겹쳐 재생하지 않는다.
- 계약·구매 확인음은 파일 요청 없는 Web Audio 합성음이며 BGM master, 음소거,
  자동재생 잠금, 숨은 탭 정지 규칙을 그대로 따른다. 계약은 종이 snap+낮은 도장+
  승인 2음, 구매는 짧은 2음, 강화는 상승 3음, 재추첨은 종이 셔플 3음, 지도는
  경로 확정음, 다음 런 선택은 옥타브 상승음을 쓴다.
- `prefers-reduced-motion`에서는 이동·크기 변화·파편을 없애고 160ms 테두리와 정적
  봉인, 결과 문구만 남긴다. 재추첨 실패도 흔들지 않는다. 소리는 사용자가
  음소거하지 않았다면 유지한다.
- 선택 입력 뒤 첫 시각 반응은 다음 렌더 프레임, 효과음 시작은 80ms 이내이며 전체
  선택 연출은 360ms 안에 끝난다. 이 상태는 `RunState`와 전투 판정에 넣지 않는다.
- 포인터와 키보드는 같은 행동 래퍼를 호출하며, 공용 입력 판별 함수의 포인터·키보드·
  다중 포인터·리사이즈 경계를 Node 회귀 테스트로 고정한다. 테스트에 게임 전이
  로직을 복사하지 않는다.
- 접근성 DOM 계약 버튼이 focus되면 같은 인덱스의 Canvas 카드에 금색 3px focus ring을
  표시한다. hover의 주황 테두리·4px 상승과 구별하고, focus가 카드 사이를 이동하거나
  계약 화면을 벗어나면 ring도 같은 프레임에 이동·해제한다.

### Information panels

- 넓은 화면의 왼쪽 패널은 `팀 현황 / 시너지 목표`, 오른쪽 패널은
  `다시 뽑기·유물 / 카드` 순서로 읽는다.
- 패널 외곽선은 `rgba(239,224,198,0.18)`, 내부 섹션 구분선은 같은 색의
  0.14~0.18 알파를 기준으로 한다. 배경과 합쳐지는 0.1 미만 선은 쓰지 않는다.
- 안내 띠는 내용이 있을 때 반투명 면과 위·아래 경계를 함께 보여 HUD, 보드,
  본문의 위계를 분리한다.

### Action dock

- 주 행동 버튼은 보드나 하단 정보 띠에 바로 붙이지 않고 별도 행동 구역으로 읽힌다.
- 행동 구역의 상단 구분선은 버튼 폭과 맞추고, 실제 히트 영역은 기존 기하를 유지한다.
- 게임오버의 세 갈래는 보조 버튼이고 화면 하단의 `다시 도전`만 주 CTA다.

### Vulnerable finisher economy

- 보스 취약 창은 **창 기회 2개**를 열고 결정타와 그 창 안의 방어가 함께 쓴다.
  별도 버튼은 만들지 않는다. 실제로 이동할 아군이 있는 예고는 산개·집결이 먼저이며,
  모두 안전한 상주 장판은 결정타를 막지 않는다.
- 결정타는 보스 최대 체력의 7% 피해를 주고 취약 창 하나당 최대 2회만 성공한다.
  취약 시간·보스 체력·문턱·회피 지급량은 이 계약과 함께 조정하지 않는다.
- 성공한 결정타는 창 기회만 1개 쓰며 전투 전체 회피 차지를 쓰거나 늘리지 않는다.
  성공한 산개·집결은 창 기회를 먼저 쓰고, 소진됐으면 기존 회피 차지를 쓴다.
  실패한 이동과 순차 스윕의 두 번째 무료 파동은 어느 자원도 쓰지 않는다.
- 창이 닫히거나 최종 채널·사망·전투 종료로 넘어가면 남은 창 기회는 반환 없이
  소멸한다. 정상 창에서 0회 공격, 1회 공격·1회 방어, 2회 공격의 결과가 실제로
  갈리는지는 정상 창만 분리한 홀드아웃 관문이 승인한다. 최종 창은 새 예고가 없는
  피니시 보장이라 이 선택성 증거에 넣지 않는다.
- 버튼은 `결정타 · 기회 2` → `한 번 더 · 기회 1` → `기회 소진 · 회피 N`으로
  남은 창 자원을 색 외 텍스트로 알린다. 창 안 방어는 `산개! 기회 N` 또는 창 기회가
  없을 때 `산개! 회피 N`처럼 실제 소비 원천을 보여준다.
- 포인터 클릭 한 번과 키보드 최초 keydown 한 번은 같은 결정타 1회다. 취약 창에서
  Space/G의 OS 자동 반복은 무시해 한 번 누르고 있는 것만으로 두 차지가 모두
  소진되지 않게 한다. 일반 예고에서의 반복 입력 계약은 유지한다.
- `act`는 입력 큐에 들어갈 때 산개·집결·결정타 의미를 확정한다. 회피 쿨다운에
  기다리는 동안 예고가 끝나고 취약 창이 열려도 이미 누른 방어 입력이 결정타로
  바뀌지 않는다.
- 공용 `strikeUsable` 판정을 전투 소비, 버튼 활성화, 입력 반복 게이트가 함께 쓴다.
  화면과 헤드리스 정책은 모두 `act → resolveIntent → doStrike` 생산 경로를 통과하며
  측정 전용 직접 `strike` 사본을 두지 않는다.
- 취약 시간이 남아도 보스가 순간이동으로 판에서 사라진 동안은 버튼을 `…`로 잠근다.
  남은 기회를 `기회 소진`으로 잘못 표시하지 않으며 복귀하면 같은 잔량을 다시 보여준다.
- 결정타는 새 난수를 소비하지 않고 `actCooldown`을 새로 걸지 않는다. 성공한 공격만
  창 기회를 소비하며, 유효하지 않은 보스·채널·순간이동 부재에는 비용도 연출도 없다.

### Active contract chip

- 상점·배치: 다음 보스 이름과 계약명을 함께 보여 대비 근거로 쓴다.
- 전투: 보스 HUD 아래 계약명과 핵심 대응 한 줄만 남긴다.
- 게임 오버: 마지막 보스 계약과 성공/실패 맥락을 부검에 포함한다.
- 선택 직후: 주소에 공유 가능한 `?raid=` 코드가 반영되며 칩에 짧은 코드를 표시한다.
- 계약 직후 지도: 위험별 추천 노드를 금색 이중 고리와 텍스트로 함께 표시한다.

### Codex proof panel

제품 런타임 안에 개발자 대시보드를 억지로 넣지 않는다. 제출 영상과 문서에서 다음
파일을 나란히 보여 준다.

- `scripts/raid-contract-candidates.json`: Codex 제안 원본
- `scripts/raid-contract-adversarial.json`: 범위 초과·금지 토큰·중복 등 적대 입력
- `scripts/gen-raid-contracts.mjs`: 검증 후 출고 데이터 생성
- `src/data/raid-contracts.json`: 실제 게임이 읽는 채택 결과
- 검증 로그: 몇 개가 왜 거절되고 무엇이 클램프됐는지

## 9. Accessibility

- Canvas는 시각 표현을 유지하되, 한 개의 단계별 DOM 접근성 브리지가 현재 결정만
  의미 구조로 투영한다. 계약·지도·상점/배치·전투·게임오버·음소거가 모두 같은
  브리지를 사용하며 지난 단계의 컨트롤은 DOM과 접근성 트리에서 즉시 제거한다.
- DOM 컨트롤, 숫자키·문자키, Canvas 포인터는 모두 `dispatchGameInput`의 같은
  phase lock과 `executeGameInputAction` 경계를 통과한다. 네이티브 버튼에 포커스가
  있을 때 전역 keydown은 Enter/Space를 다시 처리하지 않아 한 입력이 정확히 한 번만
  실행된다.
- 포인터와 키보드 `1/2/3`으로 동일하게 계약·지도·구매·게임오버 선택을 실행한다.
  재추첨은 버튼과 `R`, 전투 개입은 버튼과 `Space/G`, 음소거는 토글과 `M`이 같다.
- 구매·재추첨·지도 이동·단계 전환·게임오버 결과는 `role="status"`인 polite live
  region에서 상태 변화당 한 번만 알린다. 동일 서명은 반복하지 않고, 숫자가 매
  프레임 바뀌는 값은 발표하지 않는다.
- 전투 예고는 별도 `role="alert"`에서 의미가 바뀔 때만 한 번 발표한다. 산개·집결·
  극성·결정타 기회를 말로 구분하고 같은 예고의 남은 시간 변화는 재발표하지 않는다.
- 상점·준비의 5×5 아군 보드는 `role="grid"`와 roving tabindex를 사용한다. 화살표로
  셀을 이동하고 Enter/Space로 집기·놓기, Escape로 취소하며 포인터 드래그와 같은
  `move-cat` 행동을 호출한다. 셀 이름에는 행·열·고양이·레벨 또는 빈 칸을 함께 쓴다.
- 단계가 유지되면 안정된 `data-focus-key`로 포커스를 보존한다. 컨트롤이 사라지면
  다음 유효 선택으로 보내고, 단계가 바뀌면 새 단계의 첫 결정 컨트롤로 이동한다.
  Canvas로 강제 복귀시켜 문맥을 끊지 않는다.
- 위험도·대응·보상을 색 하나로 구분하지 않는다.
- 모든 핵심 텍스트는 시스템 한글 폰트와 충분한 대비를 사용한다.
- `prefers-reduced-motion`에서는 Canvas 연출뿐 아니라 DOM transition과 active
  transform도 제거한다.
- 숨겨진 탭 복귀, 반복 키 입력, 계약 중 Enter 입력이 상태를 건너뛰지 않게 한다.
- 최소 터치 영역 44×44px, 계약 카드는 그보다 크게 유지한다.
- 작은 화면에서도 규칙·대응·보상 항목 자체를 제거하거나 글자를 가로 압축하지
  않는다. 높이 380px 미만인 극저높이 또는 카드 폭 160px 미만인 압축 카드만
  항목별 말줄임을 허용하되 숨은 접근성 버튼에는 전체 문장을 유지한다.
- Tab으로 접근성 계약 버튼을 읽을 때 Canvas의 같은 카드에도 focus ring을 표시해
  보이는 선택과 스크린리더용 의미 구조의 순서를 일치시킨다.
- 브라우저 확대를 막는 `user-scalable=no`와 `maximum-scale=1`을 사용하지 않는다.
- 핵심 본문은 12px, 결과 화면의 보조 정보는 11px 아래로 축소하지 않는다.
- 지도에서 현재 선택 가능한 길은 `1~4 + 종류`를 항상 표시해 터치와 키보드가
  같은 선택지를 보게 한다.

## 10. Responsive behavior

- 900px 이상: 중앙 3열 카드, 각 카드 폭은 전체의 약 28%, 카드 간격 3%.
- 600~899px: 가로면 3열, 세로면 1열 스택. 횡스크롤은 쓰지 않는다.
- 599px 이하 또는 세로형: 카드당 112px과 12px 간격을 확보할 수 있을 때 3개를
  세로로 쌓는다. 그보다 낮은 화면은 폭이 600px 미만이어도 3열을 유지하며,
  한 행 header의 8px 간격 예산이 부족하면 선택 번호를 제목에 합친 두 줄 header를
  쓴다. 180px 미만 카드는 footer 키 힌트도 생략하고 보상 문구를 축약한다. 좁고
  높은 카드도 본문을 2줄보다 늘리지 않고 카드 높이를 줄여 죽은 공간을 만들지 않는다.
- 단, `320×480`·`390×500`처럼 세로이면서 540px 이하인 화면은 계약 오버레이가
  평소 하단 행동 영역까지 사용하고 104px 이상 가로 티켓 3장을 쌓는다. 이때 별도
  `카드를 누르거나 1·2·3` 안내는 숨기고 카드 안 번호만 남겨 중복 affordance를 줄인다.
- 720px 이상 세로형은 카드 156~168px, 560~719px는 136~148px을 목표로 한다.
  그보다 낮으면 가용 높이를 세 장이 나눠 갖되 카드당 104px과 핵심 정보 12px 하한을
  우선한다. 세로 카드는 `번호·제목·위험 / 보스 변화 / 내 대응 / 보상·선택` 4행
  티켓으로 접는다.
- 900×320 극저높이: 제목·부제를 한 줄로 합치고 카드 설명은 사용 가능한 높이에
  맞춰 항목별 최대 1줄을 쓰되 위험, 규칙, 대응, 보상은 모두 남긴다.
- 300px 미만 높이: 계약 제목을 안내 띠 안으로 올려 카드 본문의 세로 공간을
  확보한다.
- 극저높이 게임오버는 세부 항목을 한 줄 요약으로 합쳐 글자 크기 대신 정보 밀도를
  줄인다.
- 360px 미만이면서 세로줄을 세울 수 없는 가로 화면은 전투·배치 셀 24px을
  보존하기 위해 보조 정보인 시너지 띠를 접는다. 핵심 전투·계약·행동은 남긴다.
- 접근성 브리지는 작은 화면에서 좌우 8px, 그 외 12px 안전 여백 안에 머물고
  `max-height`와 내부 스크롤을 사용한다. 컨트롤 높이는 44px 이상, 형제 간격은
  작은 화면 8px·그 외 12px 이상이다.
- Canvas 상점 카드 사이 간격은 모든 지원 화면에서 8px 이상이다. 간격을 늘려도
  카드 히트 영역과 렌더 기하는 같은 `Layout` 값을 공유한다.
- 기존 10기기와 568×320·599×359·390×500·375×320·320×480·540×500 임계 화면,
  사용자 제보 캡처와 같은 1901×891을 `layout-probe`로 통과해야 하며 계약 카드·
  지도 라벨·행동 영역 겹침도 같은 검사에 포함한다.

## 11. Interaction states

| 상태 | 표시 | 입력 |
|---|---|---|
| 보스 노드 선택 전 | 기존 지도와 보스 힌트 | 노드 클릭 또는 숫자 |
| 계약 제안 열림 | 3개 카드, 다른 지도 입력 차단 | 카드 클릭 또는 1/2/3 |
| 계약 선택 직후 | 선택 봉인 피드백 후 기존 보상 화면 | 중복 선택 무시 |
| 지도 노드 선택 직후 | 새 화면 위 선택 위치 경로 고리·확정 봉인 | 중복 선택 무시 |
| 보스 보상 | 유물 3장 택1·건너뛰기 → 일반 상점 | 카드 클릭 또는 건너뛰기 |
| 재추첨 성공 | 새 카드 테두리 sweep·새 제안 봉인 | 버튼 또는 R |
| 재추첨 실패 | 정적 호박색 경계·기존 실패 안내 | 버튼 또는 R |
| 상점·배치 | 다음 보스 + 선택 계약 칩 | 기존 구매·배치 |
| 계약 보스전 | 보스 HUD + 핵심 대응 | 기존 Space/G |
| 승리 | 기본 보상 + 계약 추가 보상 명시 | 다음 단계 |
| 패배 | 계약명과 실패 맥락 부검 | 재도전 선택 |
| 다음 런 선택 직후 | 새 화면 위 선택 종류 봉인 | 같은 판·오늘·도전 버튼 또는 1/2/3 |
| 데이터 오류 | 안전한 기본 계약으로 폴백, 디버그 로그 | 플레이 차단 금지 |

접근성 브리지는 위 표의 입력을 복제하는 별도 게임 로직이 아니다. 현재 상태에서
버튼 이름·설명·단축키와 `GameInputAction`만 파생하고, 성공·실패 결과는 실제 전이 뒤
`RunState`의 변화와 notice를 읽어 발표한다.

## 12. Content and voice

- 제목은 2~7글자 명사구: “갈라진 밤”, “번지는 어둠”, “무너지는 바닥”.
- 규칙은 보스 주어: “보스가 판을 양쪽 규칙으로 가릅니다.”
- 대응은 명령형: “빛나는 쪽을 읽고 산개/집결을 직접 고르세요.”
- 위험은 형용사 하나: 낮음, 중간, 높음.
- 보상은 수치로: “승리 시 생선 +4”.
- 실패 메시지는 비난하지 않고 다음 시도를 가르친다.
- 제출 설명은 기여 경계를 명시한다:
  “기존 프로토타입을 바탕으로, 챌린지 제출 버전의 핵심 차별화 루프인 악몽 계약
  시스템은 Codex가 분석·설계·구현·검증했습니다.”

### 3분 데모 고정 동선

- 기준 시드: `424242` (구현 후 첫 계약 제안과 패턴 노출을 검증해 최종 확정)
- 선택 계약: “갈라진 밤”
- 0:00~0:12 — 첫 화면의 계약 세 장과 선택 이유
- 0:12~0:38 — 계약 직후 위험 ‘중간’ 이중 고리인 가위눌림 경로와 보장 유물 선택
- 0:38~1:05 — 배치와 첫 일반 전투는 2~4배속 편집
- 1:05~1:50 — 계약 보스 실제 속도, 극성의 산개/집결 수동 선택
- 1:50~2:10 — 계약 보너스 또는 실패 부검, 주소의 공유 코드
- 2:10~2:35 — Codex 후보에서 불법 토큰이 거절되고 채택 JSON이 만들어지는 과정
- 2:35~3:00 — 6축·불변식·16기기·공개 URL 검증 결과와 기여 경계

컷 편집은 지도·상점·일반 전투에만 허용하고, 계약 선택과 계약 보스의 첫 패턴부터
판정까지는 실제 속도를 유지한다.

## 13. Implementation constraints

### 하드 제약

- 런타임 외부 네트워크 요청 0건
- 런타임 의존성 0개 유지
- 기존 Canvas 2D, 세로·가로 단일 코드 경로 유지
- 검증하지 않은 생성 데이터 런타임 유입 금지
- 허용 패턴 토큰 외 값 거절, 수치 범위 클램프, id 중복 거절
- 계약 선택 전 적 웨이브 생성 금지
- 계약 추가 보상은 승리할 때만 지급
- 같은 `(seed, stage, boss ordinal)`은 같은 3개 제안과 같은 순서를 생성
- 일반/정예/정찰의 기존 상태 전이와 보스 외 전투 규칙 보존
- 준비 경로는 기존 노드 보상을 재사용한다. 낮음은 일반 전투, 중간은 정예 보장
  유물, 높음은 정찰 회피 2회 + 계약 대비 1회다. 정찰 회피는 고위험 계약의
  추천 경로에서만 지급하고 여러 정찰은 중첩하지 않는다. 추천 밖의 경로를 고르면
  입력 자원을 빼앗지 않고 대상 계약 보스의 체력·광역 피해만 1.03배로 보정한다.

### RaidContract 실행 스키마

```ts
interface RaidContract {
  id: string;                    // `[a-z][a-z0-9_]{2,23}`, 중복 거절
  name: string;                  // 2~7글자, 꺾쇠 제거
  risk: 1 | 2 | 3;               // 카드 발톱 수와 위험 텍스트
  rule: string;                  // 보스가 바꾸는 것, 최대 34자
  counter: string;               // 플레이어 대응, 최대 34자
  rewardFish: number;            // 정수 2~6, 범위 밖은 클램프
  patterns: BossPattern[];       // 3~4개, 허용 토큰 외 값이면 계약 전체 거절
  phase2Patterns?: BossPattern[];// 3~4개, 스테이지 우두머리 후반부에만 사용
  proofCode: string;             // 출고 데이터 추적용 고정 식별자
}
```

- 선택 계약의 `patterns`는 대상 보스의 기본 `patterns`를 **교체**한다. 새 기하나
  런타임 코드는 만들지 않고 기존 토큰의 실행 경로를 재사용한다.
- `phase2Patterns`가 있으면 스테이지 우두머리의 기본 후반 배열을 교체하고, 없으면
  계약의 `patterns`를 후반에도 쓴다.
- 순간이동 주기와 취약 시점·길이는 원래 보스 정체성을 보존한다. 위험 1은 원래
  `power`, 위험 2는 `×1.12`, 위험 3은 `×1.50`을 체력·광역 피해에 함께 적용해
  추가 보상과 실제 난도를 맞바꾼다.
- 보상은 대상 보스를 이긴 직후 기본 보스 보너스에 더하고, 패배·일반전에 지급하지 않는다.
- 계약은 선택 당시 계산한 `targetBossIndex`와 실제 보스 인덱스가 같을 때만 적용한다.
- 출고 JSON은 정확히 6개가 모두 유효할 때만 원자 채택한다. 실패하면 서로 다른
  안전 계약 3장으로 전체 폴백하고, 상태·거절 사유를 console/debug에 남긴다.
- 공유 코드가 유효하지 않으면 새 런으로 폴백하고 주소의 잘못된 값을 제거한다.

### 완료 관문

1. `typecheck`, 계약 적대 테스트, 기존 검증기 테스트 PASS
2. `probe`, 오디오, 에셋, 회피, 불변식, 빌드 PASS
3. 6축 측정 결과를 변경 전·후로 기록하고 의도치 않은 퇴행 설명 0건
4. 홀드아웃 시드 `10001..11800`에서 계약 인지 정책이 보상 숫자만 보고 기존
   안전 경로·기존 보스 대형을 쓰는 정책보다 생존 중앙값 **+1.0악몽 깊이 이상**
   또는 첫 두 보스 통과율 **+5%p 이상**이어야 한다. 두 정책 모두 전투 중 공개
   예고에는 반응하므로 일부러 못 싸우는 기준선을 만들지 않는다.
5. 출고 계약 6개를 각각 홀드아웃의 모든 시드에서 첫 두 보스에 연속 적용해 독립 실행한다. 계약별 평균 깊이
   최대-최소는 **4.0 이하**, 1위의 95% 구간은 2위와 겹쳐야 하며, 각 계약의 첫 두
   보스 통과율은 **70~97%** 안에 있어야 한다. 경로 선택이 전투 RNG 소비를 바꾸므로
   동일 시드 순위나 제안 노출률로 나눈 승점은 지배도 근거로 사용하지 않는다.
6. 같은 시드 반복 실행의 계약 제안·최종 요약 해시 일치
7. 데스크톱·모바일·900×320에서 포인터/키보드 계약 선택 실동작
8. 공개 Pages에서 새 계약 루프와 번들 해시 확인
9. 커밋마다 실행 명령, 종료 코드, 시드 범위, 핵심 지표를 증거 문서에 기록
10. 정적 카피 검사(이름 7자, 규칙/대응 34자), 17기기 12px 글자 하한, 900×320
    겹침 0을 통과하고 실제 브라우저에서 첫 화면·키보드·포인터를 한 번 확인
11. `docs/codex-contribution.md`에서 `문제/요청 → Codex 산출물 → 거절·채택 판단 →
    커밋 → 검증 → 사람이 결정한 것`을 한 표로 연결
12. 지도 1500런에서 정책 평균 효과가 귀무 잡음보다 큰 1.0웨이브 이상이고,
    읽는 정책은 무작위 이상·고정 최선과 0.5웨이브 이내이며 실제 준비 3경로의
    접근·보상 전달은 300시드 전부 성공
13. 유물 드래프트는 보스 승리 이벤트마다 중복 없는 3장·택1을 열고, 선택·건너뛰기
    모두 같은 보상 국면의 일반 카드 3장으로 이어져야 한다. 전투·정예·정찰 준비
    경로별 노출이 같아야 한다. 전투 전용 지도에서 2000시드로 직업 집중 정책과
    동일 정책의 무유물 대조군을 함께 재며,
    최선 집중 빌드는 일반 무유물 기준보다 **4.0 이상**, 집중 빌드 최저 이득은
    **1.0 이상**, 상위 두 직업 격차는 **1.5 이하**여야 한다. 유물만 켜고 끈
    한계효과는 평균 **+0.5 이상**, 최저 **-0.25 이상**, 5직업 중 3직업 이상이
    **+0.5 이상**이어야 한다. 모든 유물 카드는 조건·정확한 이득·대가를 표시한다.
14. 공용 입력 판별 회귀 테스트에서 포인터·키보드의 같은 행동 매핑, 두 번째 포인터
    무시, 접힌 카드 구역의 빈 슬롯 삼킴, 리사이즈 뒤 새 기하 사용을 자동 검증한다.
15. 실제 브라우저 접근성 트리에서 현재 단계 컨트롤만 존재하고 이름·단축키·Tab 순서가
    모델과 일치해야 한다. DOM click/Enter/Space는 같은 행동을 한 번만 만들며 phase
    lock을 우회하지 않는다. live region은 idle 중 0회, 상태·전투 신호 변화당 1회만
    갱신되고 reduced-motion CSS·44px 터치 영역·8/12px 간격을 자동 검사한다.

## 14. Resolved decisions

- 계약 추가 보상은 위험별 2~6 생선이며 위험 1은 2로 통일했다.
- 기본 헤드리스 정책은 낮은 위험, 계약 공간 상한 정책은 로스터·패턴을 읽는다.
- 공유는 `?raid=` 자동 로드를 출고 범위로 삼고 별도 클립보드 권한은 요구하지 않는다.
- 고정 영상은 `?seed=424242`에서 세 장을 보여 준 뒤 선택 순간 `?raid=`로 전환한다.
- 별도 훈련 모드는 만들지 않고 첫 계약 카드와 지도 이중 고리로 30초 안에 규칙을 가르친다.

## 15. Open questions

- 현재 열려 있는 구현 결정은 없다.
- 이번 자동 개선 작업은 수동 플레이 확인과 제출 문안·영상·사이트를 완료 조건에서
  제외한다. 브라우저 접근성 트리·입력·레이아웃·모션 검사는 자동 하네스로 봉인한다.
